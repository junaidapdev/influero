---
description: Review the last-built feature against plan, architecture, and standards (reports, does not fix)
argument-hint: [feature name, optional]
---

Follow `.agents/skills/review/SKILL.md` exactly. Report findings by severity and
STOP — do not fix anything unless I ask.

Note: this project is not a git repo with PRs, so review the working changes
directly (read the relevant files) rather than using `gh pr` commands.

Focus: $ARGUMENTS
