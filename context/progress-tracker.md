# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** ✅ **v1 SHIPPED (2026-06-13)** — all 5 phases / 17 features complete, deployed to Vercel (**https://influero-beta.vercel.app/**), prod auth + tenant isolation verified live. **Post-v1 — Deal Lifecycle Redesign (2026-06-13): ✅ BUILT + reviewed (adversarial workflow + a `/review` pass; all findings addressed), static-verified (typecheck + lint + build clean). Pending its LIVE GATE — apply `0013` and run through the worklist.** Separate shoot/post dates + a "today's work" worklist of shoot/post reminders + a Shot/Posted two-checkmark flow; `in_progress`→`shot`; `deadline`→`post_date`. Full build summary + the one open item (non-atomic cancel — developer's call) + the live-gate checklist are under "Decisions Made During Build" (the SHIPPED note).
**In progress:** **17 Polish + Deploy — ✅ COMPLETE (v1 shipped).** standards sweep done (static-verified). Swept the codebase against the standards checklist: **`console.*` zero** outside `lib/logger.ts` (frontend + backend), **`any` zero** (frontend + edge fns), **one response envelope** (both edge fns return only via `_shared/api.ts` `ok()`/`fail()` — no hand-built `Response`), **consistent HTTP codes** (both pull the `_shared/constants.ts` `HTTP` map: 200/400/401/404/409/429/500), **one zod** (`features/*/*.schema.ts` v3 frontend + `npm:zod@3` edge, `zodResolver` everywhere). **Type scale tokenized (the one finding fixed):** the §Typography sizes Tailwind's default scale doesn't cover were inlined as ~65 `text-[NNpx]` across nearly every component (a NEVER-rule "no hard-coded font sizes" gap — the values were spec-accurate but un-tokenized). Added `--text-{hero,row,body,caption,micro}` to `index.css` `@theme` (hero 28/34 carries its leading; the rest font-size-only so inherited line-heights — and thus rendering — are pixel-unchanged, verified in the built CSS) and swapped every arbitrary value → token; `ui-tokens.md` (@theme + §Typography utilities table + invariant) and `ui-registry.md` (all class strings + a conventions note) updated to match. `text-[11px]` (below the documented 12px floor, 5 sites) became the `text-micro` token. typecheck + lint + production build all clean; main bundle 752.65 kB (unchanged), recharts/pdf.js chunks untouched. **Acceptable non-violations left as-is:** `GoogleIcon` brand hex (a logo must use Google's exact colors), safe-area `env()` calcs (can't be tokens), `max-w-[640px]` page-column + one-off structural dims (`h-[78px]` etc.). **States audit done (loading/empty/error coverage):** all 14 primary user-facing lists/sections pass all three states (Today + Needs-attention even handle partial-query failure; IncompleteProfileBanner renders nothing on load/error by design). 4 gaps found, all a missing ERROR branch on a SECONDARY/auxiliary query (no primary list affected) — **developer chose to fix ALL 4 (done, static-verified):** (1) `SnapReportSheet` `useSnapSignedUrl` image preview → added a `signedUrl.isError` inline fallback box (`snap.detail.previewError`) so a failed source-screenshot fetch shows a message, not a blank; (2) `useDealsIndex` in brands/index → inline `text-text-muted` notice above the list when it errors (`brands.dealCountsUnavailable`); deal-count badges still degrade to em-dash; (3) deals → inline `filtersUnavailable` notice after the filters when `indexQuery`/`brandsQuery` error, PLUS the Add sheet now distinguishes `brandsQuery.isError` (→ `deals.brandsLoadError`) from genuinely-empty (so a brands-load failure no longer masquerades as "add your first brand"); (4) `useMonthlyTotals` sparkline in MonthTotalsBar → error now explicitly guarded (`!isError`) with a comment — hidden by design (no error UI on the hero gradient), but handled, not swallowed. All inline/declarative (no effect-toasts → no exhaustive-deps/spam risk); 5 new i18n keys en+ar. typecheck + lint + build all clean. **375px + RTL sweeps — ✅ CLEARED 2026-06-12 (developer-confirmed live):** every page walked at 375px width AND in Arabic RTL — pages 07–16B plus the nav shell (tab bar, FAB Quick Add sheet, profile menu, greeting header, Insights tabs), directional icons/chevrons mirroring, the calendar grid's RTL column flow, Arabic SAR placement, Arabic-Indic digits on the chart axes, and the exported snap-report PNG's Arabic shaping — all confirmed working. This blanket pass clears every per-feature "not yet LIVE-eyeballed" visual gate accumulated since Feature 07. **Also this session:** repo pushed to GitHub (github.com/junaidapdev/influero, `main`); the shell header's notification bell now links to /payments (`AppLayout` — registry updated). **Vercel deploy — ✅ DONE 2026-06-12 (developer-confirmed working):** frontend deployed to Vercel (root dir `frontend`, SPA rewrite via `frontend/vercel.json`, prod `VITE_SUPABASE_*` env set); prod Google sign-in confirmed working after fixing Supabase Auth → URL Configuration (Site URL was the localhost:3000 default — the fallback Supabase redirects to when the requested redirect isn't allow-listed; set Site URL to the prod domain + added `https://<domain>/auth-callback` to Redirect URLs; Google console needed no change — Google only ever redirects to the project's `/auth/v1/callback`). **Final 2nd-user RLS spot-check — ✅ CLEARED 2026-06-13 (developer-confirmed on the live app):** signed in as a second account; tenant isolation holds everywhere — user B sees an empty app (zero brands/deals/payments/meetings/reminders/snap reports, dashboard zeros) and cannot read user A's rows or storage paths. This was the LAST gate. **🚀 FEATURE 17 COMPLETE — INFLUERO v1 IS SHIPPED.** All 17 features done; deployed to Vercel; prod Google OAuth working; RLS verified live. **Live URL: https://influero-beta.vercel.app/**
**Last completed:** **16B Snap Report Generation** (developer-directed, added to build-plan 2026-06-12; architect-planned via a 4-question decision round, developer-approved; static-verified — typecheck + lint + production build + `deno check` on both edge functions all clean). Two report products on the Feature 15 extraction pipeline (no Snapchat API — screenshots + GPT-4o only): a **monthly office-account report** (one screenshot of Snapchat's MONTHLY Insights page → the account six: views, reach, story_views + NEW `profile_views`, `new_followers`, `watch_time_minutes`) and the **24-hour post report** (the existing per-content report, now exportable for the brand). Migration **`0012_snap_report_types.sql`** = `report_type text not null default 'post' check in ('post','monthly')` (default backfills existing rows) + the three nullable monthly columns; `report_date` semantics PER TYPE (snap date for post; FIRST day of the covered month for monthly). **`extract-snap-report`** now switches prompt + strict json_schema on the ROW's `report_type` (never client input — the storage-path stance): `MONTHLY_JSON_SCHEMA`/`MONTHLY_SYSTEM_PROMPT` (bilingual glossary incl. profile views/new subscribers/watch-time-in-minutes; month normalized to first-of-month) beside the renamed POST pair; shared prompt rules (digit/abbreviation conversion + injection defense) extracted to one const; guards/rate-limit/zod re-validation unchanged; per-type activity summary ("Snap monthly report · YYYY-MM"). **24h auto-reminder:** `useToggleDeliverable` arms the deal's `kind='deliverable'` reminder (its FIRST writer — the kind already existed in 0008's CHECK) due now+24h via new `buildSnapAnalyticsReminderMessages` + `snapAnalyticsReminderDueAt` (`features/reminders/`); createReminder's upsert-by-(kind,ref) dedup = ONE live reminder per deal re-armed per posting; **best-effort by decision** (try/catch + logger — a reminder failure never fails the visible toggle, unlike meetings where the reminder IS the contract); unmark leaves it; REMINDERS key invalidated. **UI:** /analytics/snap upload card gains a report-type `FilterChips` picker + per-type hint (insert carries `report_type`); history rows a neutral type chip + monthly rows title with `formatMonthYear`; the detail sheet renders the TYPE's field set (post five / monthly six) through the same pencil→manual flow (snap.schema gains the three monthly count fields; unrendered fields round-trip), date row labelled per type, link-to-deal HIDDEN on monthly; once extracted/manual a **Report preview** renders the new branded **`SnapReportCard`** (ProfileAvatar + display name, per-type title, brand·deal / month context, 2-col metric tile grid, "Generated with Influero" footer, 4px accent top strip; tokens only) inside a bg-background capture wrapper + a Download button → **lazy `html-to-image`** (`toPng`, pixelRatio 2, its own ~14 kB chunk — the pdf.js/recharts precedent) → `snap[-monthly]-report-<date>.png`. New pure `features/brands/brandName.ts` (`localizedBrandName`). i18n `snap.type/card/*` + new field labels (en+ar). Bundle: main ~753 kB (≈+7 kB); recharts/pdf.js chunks untouched.
**Previously:** Navigation slice (the previously-unowned app shell; developer-approved decisions; UI-only — no backend/migration/edge fn; static-verified — typecheck + lint + production build clean). New `components/layout/AppLayout.tsx` mounts as ONE protected LAYOUT ROUTE wrapping every in-app page — App.tsx refactored from 9 per-route `ProtectedRoute` wrappers to a single `<Route element={<ProtectedLayout/>}>` (= `ProtectedRoute` → `AppLayout` → page `Outlet`). The shell = a slim sticky top-trailing **profile-avatar header** (the "minimal" decision — pages keep their own titles) + the page content with fixed-bar bottom clearance + the **bottom tab bar** + the Quick Add / profile menu sheets. `MobileTabBar` = the five ui-rules slots (Home · Deals · [+FAB] · Calendar · Insights; active via pathname, Insights active on /reports OR /analytics/snap); `QuickAddSheet` = the 2×2 FAB sheet (Deal/Meeting/Payment/Snap) whose tiles navigate + (via new `hooks/useQuickAddOpen.ts`, an **effect-based** `location.state.quickAdd` trigger that fires even when already on the target route) auto-open each route's EXISTING Add sheet — Snap just navigates (its upload card IS the add surface); `ProfileMenuSheet` = the three OFF-tab destinations Brands · Payments · Settings + Sign out (`useSignOut`); `ProfileAvatar` = the shared user avatar (image / initial / `User` glyph). `InsightsTabs` (reuses `FilterChips`, navigates between routes) is the Reports↔Snap segmented control now atop both `/reports` and `/analytics/snap`. The temp dashboard quick-links row is DELETED (real nav covers every destination). New `nav.*` + `quickAdd.*` i18n (en+ar). Bundle: main ~745 kB (≈+10 kB for the shell + icons); recharts stays its own 358 kB lazy chunk (Insights reuses FilterChips, not recharts). **No new design refs** (`context/designs/` absent) — built to the ui-rules/ui-tokens nav spec.
**Previously (16 LOGIC):** 16 Reports — **LOGIC** (architect-planned, developer-approved; static-verified — typecheck + lint + production build clean). New migration `0011_report_aggregates.sql` = two read-only `security invoker` RPCs mirroring `get_dashboard_stats`: `get_monthly_totals(window_start, window_end)` (generate_series over the local 12-month window LEFT-JOINed to invoiced-by-deadline-month + collected-by-received_date-month → always 12 ordered rows, empty months as 0) and `get_per_brand_report()` (ALL-TIME per brand: deal_count + invoiced over non-cancelled deals, collected over received payments on those same deals; only brands with ≥1 non-cancelled deal; biggest invoiced first). Shared snake_case row types in `backend/shared/types/reports.types.ts`. `hooks/useReports.ts` = `useMonthlyTotals()` (computes the local window, maps to `MonthlyTotal[]`) + `usePerBrandReport()` (RPC keyed by brand_id, JOINED to the cached `useBrands` list → `PerBrandRow[]`). `/reports` wired to both hooks (mocks deleted) with per-section loading/error states; the "Last 12 months" caption moved onto the chart section (per-brand is all-time). `QUERY_KEYS.REPORTS` added; deal + payment mutations also invalidate it. **Deferred F14 folded in:** hand-rolled SVG `components/dashboard/Sparkline.tsx` (NOT recharts — keeps the 358 kB chart chunk off the landing page) renders the invoiced trend in the dashboard hero from the shared `useMonthlyTotals` series; degrades to absent until loaded, hidden when all-zero. Bundle guardrail held: recharts stays its own ~358 kB lazy chunk, main ~734 kB (≈+2 kB).
**Previously (16 UI):** 16 Reports — Monthly & Per-Brand (UI-first on mock data; static-verified — typecheck + lint + production build all clean). `/reports` = two stacked sections: the **Monthly total** Recharts bar chart (invoiced vs collected, last 12 months; RTL-tolerant axes — X reversed + Y on the trailing edge in Arabic; **lazy-loaded into its own ~358 kB chunk** so the main bundle didn't absorb recharts — the pdf.js precedent) and the **Per brand** list (static row cards: brand + deal count + invoiced SAR + a success-fill collection-rate bar; the shared `collectionRate` guard → em dash when invoiced is 0, never NaN). `recharts` v3.8.1 installed (its first + only consumer). Mock data lives in the route; the `useMonthlyTotals` + `usePerBrandReport` aggregates wire next session.
**Previously (15):** Snap Extraction (SCHEMA-FIRST; architect-planned, developer-approved; static-verified incl. `deno check` on the edge function). Migration `0010_snap_reports.sql` (table + verbatim RLS + `(user_id, created_at desc)` + `(user_id, deal_id)` indexes + **PRIVATE** `snap-uploads` bucket with own-path select/insert/delete + `alter publication supabase_realtime add table snap_reports` — closes Feature 04); edge function `extract-snap-report` (input `{ snapReportId }` ONLY — storage path comes from the row, no client URL ever (SSRF/IDOR); guards in cost order: 404 → 409 already-processed → 429 per-user hourly rate limit (`SNAP_RATE_LIMIT_PER_HOUR`, default 20) → as-user storage download → GPT-4o via plain `fetch` with strict json_schema (all six fields required-but-nullable) + bilingual data-not-instructions prompt → zod re-validation → guarded update to `extracted` (or `failed`) → `logActivity('snap_extracted')`); `/analytics/snap` = upload card (MIME + magic-byte + size validation; PDF→PNG client-side via dynamically-imported pdf.js, own lazy chunk) + report history list (realtime UPDATE subscription per user while anything is pending — no polling) + detail sheet (signed-URL image preview, per-field edit pencils → `'manual'`, failed opens straight into manual entry, link-to-deal select that never touches status); deal expanded row's snap placeholder replaced with the read-only linked-report line. Client marks a row `failed` if the invoke itself dies, so nothing sits `pending` forever.
**Previously (14):** Dashboard — Stats + Today + Needs Attention (architect-planned, developer-approved; static-verified; review minors fixed). Migration `0009_dashboard_stats.sql` (`get_dashboard_stats(month_start, month_end)` — security-invoker read-only RPC, all five month numbers in ONE round trip, caller passes the viewer-LOCAL month range); real `/dashboard` (hero "This Month" gradient card with invoiced metric + collection-rate ring + posted/pending line, three stat tiles, Today panel = FIRST READER of `reminders` with Done/dismiss (`is_done` writer decided), Needs-attention panel, "All clear" empty states, temp quick-links kept at the bottom until nav lands); brand-detail lifetime total / avg deal size / last engagement now real (client-side `getBrandRollup` over the page's one deals query). Deal + payment mutations now also invalidate the new `DASHBOARD` query-key prefix. Post-review fixes: payment/deliverable/meeting reminder rows navigate to their action pages while Done remains a separate per-row action; tomorrow items show `Tomorrow, {{time}}`/`غدًا، {{time}}`; Today uses split query hooks and partial-failure rendering; Needs-attention preserves whichever half loaded on partial failure; Today owns a small clock state for overdue transitions; dismiss pending state is per reminder row.
**Next:** 17 Polish + Deploy. **16B live gate — ✅ CLEARED 2026-06-12** (developer-confirmed in-app: `0012` applied + edge fn redeployed; monthly extraction returns sane account numbers, the type picker + per-type detail fields render, **PNG export downloads correctly**, and the **24h `deliverable` auto-reminder** fires — one row per deal, re-armed per posting). Feature 17 is the final pre-ship pass: 375px sweep, loading/empty/error states sweep, RTL sweep (directional icons/chevrons), standards sweep (zero `console.log`/`any`/inline magic values; one envelope + consistent HTTP codes; one zod), final 2nd-user RLS spot-check, then deploy the frontend to Vercel with prod env vars + Google OAuth redirect URIs on the prod domain + Supabase prod config & edge secrets. The nav slice is now DONE, so 17 has no remaining prerequisite. **Nav visual gate:** at 375px + RTL — the bottom tab bar (Home stays leading-edge, FAB centered above the baseline, active tab = accent + filled-by-color), the FAB Quick Add 2×2 sheet, the profile menu (Brands/Payments/Settings/Sign out + avatar/name header), the sticky avatar header (notch safe-area), and the Insights segmented control; confirm every page clears the fixed bar (nothing hidden behind it), Quick Add → each Add sheet opens (including from the SAME page), and sign-out from the menu lands on /login. **16 live gate (NEW):** apply `0011`; seed two brands + three months of deals/payments → every monthly + per-brand number matches a hand calculation (invoiced = non-cancelled deals by deadline month; collected = received payments by received_date month; per-brand all-time), collection rate correct, em dash on a zero-invoiced brand, the monthly chart shows 12 bars with empty months as 0, the dashboard hero sparkline trends; **16 visual gate:** /reports + the hero sparkline at 375px + RTL — Arabic axis mirroring, 12-group density, tooltip, success bars, the em-dash brand, both empty states, sparkline mirrored + readable on the gradient, Arabic SAR placement + Arabic-Indic digits on the axes; 2nd-user RLS on both new RPCs (security invoker → a 2nd user sees only their own aggregates). **15 live gate** — apply `0010`, set secrets (`supabase secrets set OPENAI_API_KEY=…` and optionally `SNAP_RATE_LIMIT_PER_HOUR`), deploy (`supabase functions deploy extract-snap-report`), then from the app: upload an ENGLISH Snap Insights screenshot → row appears pending → flips to extracted via realtime (no reload) with sane numbers; repeat with an ARABIC screenshot (Arabic-Indic digits / ألف-style abbreviations convert to plain integers); upload a PDF → converted client-side, only a PNG lands in the bucket; edit a field with the pencil → status `manual`, persists across reload, never reverts to extracted; upload a non-Insights image → `failed` → manual-entry path works; link a report to a deal → the deal's expanded row shows the snap line; exceed the hourly limit → 429 and the row flips `failed` (never stuck pending); confirm the `snap_extracted` activity row; 2nd-user RLS test on `snap_reports` AND the `snap-uploads` bucket (user B cannot read or write user A's path). **14 live gate** — apply `0009`, open the dashboard: five numbers match a hand calculation against seeded deals/payments (invoiced = non-cancelled deals with deadline this month; collected = received this month; outstanding = ALL non-received), collection ring shows — when nothing invoiced; create a near-future meeting → appears in Today (and its lead reminder is deduped, not doubled); tap Done on a payment reminder → row leaves Today, `is_done=true` in the table; overdue payment + past-deadline deal appear under Needs attention and link through; brand detail shows real lifetime/avg/last-engagement. **04 is now fully closed** (`0010` wrote its last slice). **06 LIVE-confirmed by developer.** **Remaining gates:** second-user RLS test on the `avatars` bucket; **08 live gate** (apply `0003` + smoke `logActivity` + 2nd-user RLS read on `activity_log` — partially covered by 10/11's gates); **09 live gate** — apply `0004`, create + edit a brand, then 2nd-user RLS test on `brands`; **10 live gate** — apply `0005`, create a deal, toggle deliverables both directions, confirm the three `activity_log` kinds, cancel a deal, 2nd-user RLS test on `ad_deals`; **11 live gate (the acceptance gate) — ✅ CLEARED 2026-06-12:** `0003`–`0007` applied; `tests/test_mark_payment_received.sql` → ALL 6 PHASES PASS (also confirmed visually via the new `tests/test_mark_payment_received_visible.sql` variant that SELECTs a `phase | result` grid, since the Supabase web editor doesn't render `RAISE NOTICE`); `mark-payment-received` edge function deployed; in-app mark-received flipped the ramadan deal to **paid** ("All payments received", SAR 0 pending) with `payment_received` rows logged in `activity_log`; **2nd-user RLS on `payments` confirmed** (user B cannot see user A's data); **`deal_paid` activity row confirmed** in `activity_log` — atomic write proven end-to-end, nothing left open on 11; **13 live gate** — apply `0008`, create a meeting (reminder row appears with due_at = scheduled_at − lead), edit its time (reminder moves), cancel it (reminder row deleted, meeting row stays cancelled), send a payment reminder twice (ONE row, due_at moved), confirm the `meeting_scheduled` activity row, 2nd-user RLS test on `meetings` + `reminders`. **Visual gates (375px + RTL) — ✅ ALL CLEARED 2026-06-12:** the developer walked every page at 375px + Arabic RTL (07 banner, 09 brands pages, 10 deals pages, 11 payments page, 12 expanded payment line + button pair, 13 meetings list + calendar grid, 15 /analytics/snap surfaces, 16/16B reports + exported PNG, and the nav shell) and confirmed everything working — including Arabic SAR placement and the chart axes' Arabic-Indic digits.

---

## Progress

### Phase 1 — Foundation
- [x] 01 Homepage / Entry
- [x] 02 Auth
- [x] 03 i18n + RTL Shell — **fully closed by Feature 10.** Core shipped with 02 (provider, ar/en catalogs, `<html lang/dir>` switching, `useLocale`, LocaleToggle); `lib/numbers.ts` with 07/09 (`formatPercent`, `formatNumber`); `lib/date.ts` (Hijri+Gregorian `formatDualDate` with explicit `-u-ca-…` calendars + UTC pinning, plus `formatMonthYear`) and `lib/currency.ts` (`formatSar`, `currencyDisplay: "code"`) with 10 — each built with its first real consumer.
- [x] 04 Database Schema — **`app_users` slice** (`0001_app_users.sql`) **+ `avatars` bucket slice** (`0002_avatars_bucket.sql`, F06) **+ `activity_log` slice** (`0003_activity_log.sql`, F08) **+ `brands` slice** (`0004_brands.sql`, F09) **+ `ad_deals` slice** (table + RLS + `ad_deals (user_id, status, deadline)` index in `0005_ad_deals.sql`, pulled forward by Feature 10) **+ `payments` slice** (table + RLS + `payments (user_id, status, expected_date)` index in `0006_payments.sql`, F11) **+ `meetings`/`reminders` slice** (both tables + RLS + `meetings (user_id, scheduled_at)` / `reminders (user_id, due_at, is_done)` indexes in `0008_meetings_reminders.sql`, F13) **+ `snap_reports`/`snap-uploads` slice** (table + RLS + both indexes + PRIVATE bucket + realtime publication in `0010_snap_reports.sql`, F15). **FULLY CLOSED by Feature 15.**

### Phase 2 — Profile & Settings
- [x] 05 Settings Page — Full UI (mock data)
- [x] 06 Settings Save Logic
- [x] 07 Incomplete Profile Banner
- [x] 08 Activity Log

### Phase 3 — Brands & Deals
- [x] 09 Brands Page — Full UI + Logic
- [x] 10 Ad Deals Page — Full UI + Logic + Status Machine

### Phase 4 — Money & Time


- [x] 12 Payment ↔ Deal ↔ Dashboard Wiring
- [x] 13 Meetings + Reminders
- [x] 14 Dashboard — Stats + Today + Needs Attention

### Phase 5 — Snap Analytics & Reports
- [x] 15 Snap Extraction (SCHEMA-FIRST)
- [x] 16 Reports — Monthly & Per-Brand — **UI + LOGIC complete (static-verified).** Aggregates via the `0011` RPCs (`get_monthly_totals` + `get_per_brand_report`); `useMonthlyTotals` + `usePerBrandReport` wired into /reports (mocks gone); deferred F14 hero sparkline folded in. Live gate: apply `0011`.
- [x] 16B Snap Report Generation — **complete + LIVE-CONFIRMED (2026-06-12).** `0012` report types + monthly metrics; per-type extraction in `extract-snap-report`; 24h `deliverable` auto-reminder; type picker + per-type detail fields; `SnapReportCard` + lazy PNG export. **Live gate ✅ CLEARED:** `0012` applied + edge fn redeployed; developer confirmed in-app — monthly extraction (sane account numbers), type picker, per-type detail fields, **PNG export (download working)**, and the **24h `deliverable` auto-reminder** all live. Nothing left open on 16B.
- [x] 17 Polish + Deploy — **✅ COMPLETE (v1 shipped 2026-06-13).** standards sweep DONE (console/any/envelope/HTTP/zod all clean; type scale tokenized — `--text-{hero,row,body,caption,micro}`, ~65 inline `text-[NNpx]` removed, rendering pixel-unchanged); **states audit DONE** (all 14 primary lists pass loading/empty/error; 4 secondary-query error gaps fixed); **375px + RTL sweeps DONE (developer-confirmed live 2026-06-12)**; **Vercel deploy DONE** (prod Google OAuth fixed via Supabase Auth URL config); **final 2nd-user RLS spot-check CLEARED 2026-06-13**. Nothing left — v1 shipped.

---

## Decisions Made During Build

_Add decisions here as they are made during implementation._

- Backend + frontend = separate folders in one monorepo (deliberate override of code-review point 5; rationale in `02-architecture.md`).
- Backend platform: Supabase (Postgres + auth + storage + edge functions on Deno + realtime).
- Edge runtime is Deno → no server-side PDF rasterization. PDFs converted to PNG client-side (pdf.js); edge function only ever receives an image.
- Snap result delivery via Supabase realtime, not polling.
- Dates: store ISO/Gregorian (UTC); display Hijri (Umm al-Qura) + Gregorian, Hijri leading in Arabic locale.
- Reminders created in application/edge code, not Postgres triggers (visible, testable, debuggable).
- Profile table named `app_users` (auth `users` is Supabase's; `app_users` is our extension).
- DB transactions on Supabase = Postgres functions (RPC) with `security invoker`, called from edge functions; canonical case is `mark_payment_received`.
- Edge-function ownership default: `createSupabaseAsUser` (forwards caller's JWT, RLS still applies). `createSupabaseAdmin` (service role) reserved for explicit, ownership-checked cases.
- Deploy: frontend on Vercel; backend on Supabase. Accounts already provisioned (Supabase project, OpenAI, Google OAuth).

**Feature 01 — Homepage / Entry (scaffold + entry router):**
- Frontend scaffolded under `frontend/`: Vite 6 + React 18 + TypeScript strict (project-reference `tsconfig.app.json` + `tsconfig.node.json`).
- Tailwind v4 via `@tailwindcss/vite` plugin; all design tokens live in `src/index.css` `@theme` (no `tailwind.config.ts`, per ui-tokens.md).
- ESLint flat config (`eslint.config.js`): typescript-eslint recommended + `no-console: error` + `@typescript-eslint/no-explicit-any: error`. `lib/logger.ts` is the one sanctioned `console` chokepoint.
- Deviation: the Google-Fonts `@import` is placed **before** `@import "tailwindcss"` in `index.css` — CSS requires all `@import`s to lead, and the Tailwind import expands to rules. (ui-tokens.md shows the reverse order; this is the correctness fix.)
- `@supabase/supabase-js` pinned **exact** at `2.108.1` (standards: pin this central client).
- `@types/node` added, scoped to the node tsconfig (`types: ["node"]`) so Node globals don't leak into browser code (`types: []` in the app tsconfig); needed for the `node:url` path alias in `vite.config.ts`.
- Auth session read via `hooks/useSession.ts` using `useState` + `supabase.auth.onAuthStateChange` (auth is an event subscription, not request/response data — deliberately not TanStack Query).
- Entry router `routes/index.tsx`: shows `FullPageLoader` skeleton during the auth check, then `<Navigate replace>` to `/dashboard` (session) or `/login` (no session).
- `routes/login.tsx` and `routes/dashboard.tsx` are placeholders only — real builds land in Feature 02 and Feature 14.
- Scope discipline: only Feature-01 deps installed (react-router-dom, @tanstack/react-query, @supabase/supabase-js, Tailwind/TS/ESLint tooling). zod, react-hook-form, i18next, recharts, date-fns, lucide-react are deferred to the features that introduce them.

**Feature 02 — Auth (architect-planned; bundled with i18n shell + app_users prereqs):**
- **Resequence (developer-approved):** i18n shell → `app_users` table+RLS → full Auth. Auth's login UI needs i18n, and its profile bootstrap needs the table — building strictly in number order would have shipped hardcoded strings + a non-functional bootstrap.
- **Profile bootstrap = client-side idempotent upsert** (`hooks/useEnsureAppUser.ts`), `upsert(..., { onConflict: 'user_id', ignoreDuplicates: true })` under the user's own session (RLS `insert own`). No Postgres trigger — matches the project's "application code, not triggers" stance; self-heals because idempotent; preserves later user edits. Seeds `display_name`/`avatar_url` from OAuth metadata when present.
- **OAuth = PKCE flow** (`flowType: 'pkce'` + `detectSessionInUrl: true`). `/auth-callback` waits for the session (client auto-exchanges the code on load) rather than calling `exchangeCodeForSession` manually. Has a 10s timeout fallback to `/login`. **Caveat:** PKCE email-confirmation is same-device (code verifier lives in that browser's storage) — documented in `library-docs.md`.
- **Email/password:** `signUp` with `emailRedirectTo` → when `data.session === null`, show the "check your email" VerifyNotice. Email link returns through `/auth-callback`.
- **i18n core only now:** date/currency/number formatters (rest of Feature 03) deferred to their first real consumer so they're not built untested.
- **`@shared/` alias added** (vite + tsconfig) → `backend/shared/` is in the frontend's tsconfig `include`; `AppUser` type lives there, framework-agnostic, exercised via `Pick<>` in `useEnsureAppUser`. This is the seam the future RN app reuses.
- **UI primitives are hand-rolled, token-exact** (`components/ui/{Button,Input,Label,FieldError,LocaleToggle}`) — NOT shadcn/ui. `shadcn init` against Tailwind v4 would rewrite `index.css` and risk clobbering the `@theme` tokens. Architecture.md says `components/ui` = shadcn primitives; this is a deliberate deviation. **Decision (developer-confirmed): stay hand-rolled.** Swapping to real shadcn later remains a clean, optional change.
- **tsconfig:** removed deprecated `baseUrl` (TS 7.0); `paths` resolve relative to tsconfig under `moduleResolution: bundler`. Added `resolveJsonModule` for the locale JSON imports.
- Deps added: `i18next`, `react-i18next`, `react-hook-form`, `@hookform/resolvers@^3`, `zod@^3`, `lucide-react` (all on the approved list).

**Feature 02 — `/review` fixes applied (developer said "fix all"):**
- **Auth errors now keyed on `error.code`** (`features/auth/authError.ts` + `AUTH_ERROR_CODE`/`AUTH_ERROR_MESSAGE_KEY` in `constants/auth.ts`), replacing fragile `error.message` substring matching. Verified the SDK exposes a stable `AuthError.code`.
- **`Button` defaults to `type="button"`** — submit buttons opt in with `type="submit"`. Removes the accidental-form-submit footgun.
- **One session subscription** — `SessionProvider` (`components/providers/`) + `SessionContext` (`lib/sessionContext.ts`) hold the single `getSession`+`onAuthStateChange` listener; `useSession` now reads context (throws if no provider). Wrapped in `main.tsx`. Replaces the per-call subscriptions.
- **`app_users` migration adds explicit grants** — `grant ... to authenticated` + `revoke all ... from anon`, so access no longer depends on project default-privilege config. (If you already applied `0001`, re-run those two statements.)
- **`/auth-callback` short-circuits** — with no `code`/`error` in the URL (e.g. a direct visit) it redirects to `/login` immediately instead of waiting the 10s timeout; the timeout now only arms when a `code` is actually present.
- **`index.html` ships `lang="ar" dir="rtl"`** (Arabic-first default) so there's no direction mismatch before i18n runs.
- **Resolved by decision, not code:** shadcn (stay hand-rolled — above); JS bundle 561 kB (route code-splitting deferred to Feature 17 — marginal gain now since supabase-js/react/i18next are shared by entry+login).
- **Live (developer-confirmed):** `.env.local` set; sign-in → session → `ProtectedRoute` → `/dashboard` redirect all work end-to-end (lands on the Feature 14 placeholder, as expected).
- **Still to confirm:** `app_users` row actually created on first login (proves migration applied + grants + RLS insert + client bootstrap); the **second-user RLS test**; the email-verify path and Google specifically (depending on which method was used); the 375px visual pass on the login screen.

**Feature 05 — Settings Page (UI-first, mock data):**
- `/settings` route (protected) at `routes/settings.tsx`. Sections as cards: **Language** (reuses `LocaleToggle` — live RTL preview, already functional), **Reminders** (number input, mock 60, "minutes" + "Affects future reminders only"), **Profile** (display-name input + presentational `AvatarDropzone`).
- **Sign-out is wired** (`useSignOut` → `/login`) — it's auth plumbing, not settings-save, and it unblocks the second-user RLS test. Everything else is presentational with mock values.
- New reusable primitives: `components/ui/Card.tsx`; `Button` gains a `destructive` variant (`bg-surface border border-error-light text-error-foreground hover:bg-error-lightest`). New: `components/settings/{SettingsSection,AvatarDropzone}.tsx`.
- Temp affordance: a `Settings` link on the dashboard placeholder so `/settings` is reachable until the real nav/profile menu lands in Feature 14.
- **Deferred to Feature 06 (per build-plan "no save logic yet"):** zod `settingsSchema` + react-hook-form, `useUpdateAppUser`, avatar file-pick/preview/upload + `avatars` bucket, prefill from `app_users`, saved-state toast. The reminder/name inputs are uncontrolled `defaultValue` mocks for now — 06 converts them to a real RHF form.
- Verified: typecheck + lint + build clean. 375px visual pass still to eyeball in the running app.

**Feature 06 — Settings Save Logic (architect-planned; static-verified):**
- **Mock → real form:** `routes/settings.tsx` is now a react-hook-form form (`zodResolver(settingsSchema)`, `values` for async prefill) for `display_name` + `reminder_lead_minutes`; loading skeleton while `useAppUser` resolves, inline error card on query failure.
- **New hooks:** `useAppUser` (first cached `useQuery`; `QUERY_KEYS.APP_USER` in new `constants/queryKeys.ts`) and `useUpdateAppUser` (one mutation for the form Save *and* the locale toggle; optional avatar upload → `getPublicUrl` + `?v=` cache-bust → single RLS-gated PostgREST `update` → invalidate `APP_USER`). **No edge function** — single-table write, not an atomic multi-row op.
- **`avatars` bucket = PUBLIC-READ, own-path write** (developer-approved deviation from architecture's "own paths only select"): `0002_avatars_bucket.sql` creates the public bucket + insert/update/delete RLS on `storage.objects` scoped to `avatars/{auth.uid()}/`. Justified by the existing Google-avatar precedent + signed-URL complexity. Public read only ever exposes profile images; writes stay locked per-user.
- **Avatar pipeline:** `AvatarDropzone` upgraded to a real click/drag picker (controlled, presentational); validation (`validateAvatarFile`: MIME + ≤2 MB, png/jpeg/webp) + object-URL preview lifecycle live in the route; path/MIME logic in pure `features/settings/avatar.ts`; constants in `constants/storage.ts`. **Magic-byte hardening deferred to the Snap pipeline (Feature 15)** where untrusted image *content* is the real risk.
- **Locale persists immediately on toggle** (developer-approved): `LocaleToggle` gained an additive optional `onLocaleChange`; Settings persists `{ locale }` via the same mutation right after the live i18n switch. UI stays in the new locale even if the write fails (error toast is the signal). **Cross-device read-back on app load is out of scope** — `app_users.locale` is written now; reconciling it → i18n at startup is a later task.
- **Toast primitive (app's first):** `components/ui/Toast.tsx` + `ToastProvider` (single slot, latest-wins, 3s auto-dismiss) + `lib/toastContext.ts` + `hooks/useToast.ts`, wrapped around `<App>` in `main.tsx`. `role="status"`/`aria-live="polite"`; `position: fixed` is a documented deviation from ui-rules' fixed-element allowlist (an overlay toast needs it; ui-rules itself mandates toasts).
- **Deviation — schema location:** plan said `backend/shared/schemas/`, but a bare `import "zod"` can't resolve there (backend/shared sits outside `frontend/node_modules`). Moved to `features/settings/settings.schema.ts` per library-docs ("schemas live in features/ … shared when edge functions land"). Relocates to shared/ when an edge function first consumes it.
- Verified: typecheck + lint + production build all clean (bundle ~587 kB — code-split still deferred to Feature 17).
- **LIVE-confirmed by developer:** `0002` applied + `avatars` bucket public; the core flow works (avatar upload → persisted → renders; locale persist + prefill; form save + toast). **Remaining gate:** second-user RLS test on the `avatars` bucket (user B cannot write into user A's folder) — needs a 2nd account; 375px visual pass not yet formally eyeballed.

**Feature 07 — Incomplete Profile Banner (no `/architect` — not logic-heavy; static-verified):**
- **No backend, no migration, no new hook** — the banner is a pure UI derivation over the existing cached `useAppUser` query. Reuses `QUERY_KEYS.APP_USER` (so visiting Settings then Dashboard shares one fetch). Required fields = `display_name` + `avatar_url`; completion % = filled ÷ required; the banner self-hides when both are present.
- **Pure logic lives in `features/profile/completion.ts`** (`getProfileCompletion`, React-/Supabase-free per the `features/` boundary) — returns `{ isComplete, filledCount, requiredCount, completionPct, missingFields }`. `REQUIRED_PROFILE_FIELDS` keys (`"displayName"`, `"avatar"`) double as the i18n suffix (`dashboard.profileBanner.missing.<field>`). "Filled" = non-null **and** non-blank-after-trim.
- **`lib/numbers.ts` pulled forward from Feature 03** — the banner is its genuine first consumer, so the ring's percent renders in locale digits (`50%` / `٥٠٪`) via `Intl.NumberFormat({ style: 'percent' })`. Built **`formatPercent` only**; `formatNumber` still deferred to its first consumer (Feature 14). Scoped intentionally — not the whole numbers lib.
- **New components** (registered in ui-registry): `components/dashboard/CompletionRing.tsx` (presentational SVG ring, token `stroke-*`/`fill-*`, `aria-hidden`, reduced-motion-aware arc transition) and `components/dashboard/IncompleteProfileBanner.tsx` (self-contained panel: `useAppUser` + completion derivation + local `useState` dismiss). First files under `components/dashboard/`.
- **Dismiss = local component state** (resets on next mount), NOT persisted — a still-incomplete profile re-nudges on the next dashboard visit, which is the intended behavior; no sessionStorage/localStorage pattern introduced. **Decision, not deferral.**
- **Missing-field tags are neutral chips, not status pills** — `bg-surface-secondary` / `text-text-secondary` from tokens, so ui-rules' "never invent a status-pill color" doesn't apply (these aren't deal/payment status). `uppercase` + `tracking-wide` are **`ltr:`-gated** so Arabic keeps natural case and cursive letter-joining (letter-spacing would break Arabic shaping). CTA arrow flips via `rtl:-scale-x-100`.
- **Renders nothing on load/error** — a nudge isn't essential content, so no skeleton (avoids a banner flashing in then vanishing). The dashboard *placeholder* was reshaped to the standard page shell (`max-w-[640px]` column) so the banner sits above the eventual Feature-14 top-line numbers.
- Verified: typecheck + lint + production build all clean (bundle ~591 kB). **Not yet LIVE-eyeballed** — 375px visual pass + RTL pass on the banner pending (carried with the other open visual gates).

**Feature 08 — Activity Log (architect-planned; PRIMITIVE ONLY; static-verified):**
- **Scope decision (architect): build the primitive, wire nothing.** Feature 08 has zero live call sites — all 7 event kinds belong to features not built yet (deals 09/10, payments 11, meetings 13, snap 15). So this ships the table + RLS + the `logActivity` helper + shared kinds/types; the actual `logActivity(...)` calls at the 7 event points are deferred to the features that own those events. Build-plan's "wire the log writes at the points" can't happen now because the points don't exist.
- **Migration `0003_activity_log.sql`** — `activity_log` table + 4-policy own-row RLS + `(user_id, created_at desc)` index + explicit `grant…to authenticated`/`revoke…from anon` (mirrors `0001`). Pulled forward from Feature 04's schema slice, same pattern 06 used for the avatars bucket. **`kind` has a DB `CHECK`** constraining it to the 7 canonical kinds (rejects typos / unsanctioned kinds at the DB layer; keep in sync with code-standards + `activity.types.ts`). **`user_id` defaults to `auth.uid()`** so the fire-and-forget insert never passes it; RLS still gates the write. (Note for F11: a service-role edge write has no JWT → `auth.uid()` is null → that path must set `user_id` explicitly.) Followed `0001`'s full 4-policy template (select/insert/update/delete) verbatim rather than making it append-only — the documented RLS template is uniform across every table; append-only would be an undiscussed deviation.
- **Shared `backend/shared/types/activity.types.ts`** — `ACTIVITY_KIND` const + derived `ActivityKind` union + `ActivityLog` row type. Framework-agnostic (no React/Vite/Deno) so the browser helper now and the Feature-11 edge helper later write the exact same kinds + shape. Lives in `@shared/` (not `frontend/constants/`) on purpose — the kinds are cross-runtime, same as `appUser.types.ts`.
- **`lib/logActivity.ts`** (the one sanctioned non-hook Supabase writer, like `lib/logger`) — `logActivity({ kind, summary, refId?, refTable? })`: a single `supabase.from("activity_log").insert(...)` that **never throws** (try/catch swallows + `logger.error("logActivity", …)`), omits `user_id` (DB default fills it), and does **no** TanStack Query invalidation (fire-and-forget side-effect, not cached server state; no reader until Feature 14).
- **Edge helper deferred to Feature 11** (architect decision) — the Deno `_shared/` logActivity lands with the first edge function that needs it (`payment_received`/`deal_paid`/`snap_extracted` fire from edge functions). It will reuse `activity.types.ts` verbatim. Not scaffolding `backend/supabase/functions/_shared/` now with no consumer.
- **No UI, no i18n strings, no `ui-registry.md` change, no new query key** — there is no reader and no component this session.
- Verified: typecheck + lint + production build all clean (bundle ~590 kB — `logActivity` has no importer yet so it tree-shakes out; the >500 kB warning is the pre-existing code-split deferral, not this change). **Live gate pending (developer-gated):** apply `0003`, smoke `logActivity` once from the running app under a session (confirm a row with the auto-filled `user_id`), then the 2nd-user RLS read test on `activity_log`.

**Feature 09 — Brands Page (architect-planned; the reusable CRUD pattern; static-verified):**
- **The reference CRUD chain** every later feature copies: migration+RLS (one file) → shared row type → zod schema in `features/` → one domain hook file → route with loading/empty/error/populated states + a shared form. Established here for `brands`.
- **Migration `0004_brands.sql`** — `brands` table (architecture schema) + verbatim 4-policy own-row RLS + grants/`revoke from anon` + `brands (user_id, created_at desc)` index (pulled forward from Feature 04, same pattern as avatars/activity_log). `user_id uuid not null default auth.uid()` safety net; the create hook still passes `user_id` explicitly (convenience filter; RLS enforces). **NOTE:** the architecture index list didn't enumerate a brands index — this one was added to serve the directory's newest-first list; low-risk, consistent with the activity_log index shape.
- **Shared `Brand` type** in `backend/shared/types/brand.types.ts` (framework-agnostic, like `appUser`/`activity`). **`brandSchema`** in `features/brands/brand.schema.ts` (where zod resolves; relocates to `backend/shared/schemas/` when an edge function needs it). All fields modelled as strings (RHF gives `""`); optional contact fields/notes validate format only when non-empty and map `""`→`null` at the hook (`toBrandColumns`). **Lenient phone** (`/^[+()\-\s\d]{7,20}$/`, developer-approved) — brands can be international.
- **Hooks: one file `hooks/useBrands.ts`** (per the architecture folder map) exporting `useBrands` (list), `useBrand(id)` (detail, `.maybeSingle()` so missing/cross-tenant → null → friendly not-found, not an error), `useCreateBrand`, `useUpdateBrand`. `QUERY_KEYS.BRANDS = ["brands"]`; detail keys off `[...BRANDS, id]` so create/update invalidating `BRANDS` covers both list and detail. Untyped client → `data as Brand` (sanctioned pattern).
- **Add/Edit = bottom sheet, NOT a centered modal** (developer-approved). New reusable `components/ui/BottomSheet.tsx` (scrim + slide-up panel, focus trap, body scroll-lock, Escape/scrim/close dismissal, reduced-motion; `onClose` held in a ref so a mutation re-render doesn't steal focus). Doubles as the future Quick Add container. Sanctioned by ui-rules' fixed-element allowlist (bottom-sheet scrim). **New `--color-scrim` token** (`bg-scrim`, `rgba(0,0,0,0.45)` from ui-rules) added to `index.css` **and `ui-tokens.md`** so the backdrop stays tokenized.
- **Edit IS in scope** (developer-approved) — `BrandForm` is reused create+edit; edit opens from the detail page and exercises `useUpdateBrand` for real (the "every feature testable" standard).
- **New shared primitives** (registered in ui-registry): `BottomSheet`, `Textarea` (notes), `feedback/EmptyState` (generic; every list reuses it). **Brand components:** `BrandAvatar` (deterministic tint from id via `features/brands/brandTint.ts` — six pastel tints paired with existing deeper-tone foreground tokens, no new hex), `BrandListItem` (row card = `<Link>` to detail; em-dash deal-count placeholder for Feature 10), `BrandForm`.
- **`lib/numbers.ts` `formatNumber` pulled forward** — the brand count ("8 brands" / "٨ علامة تجارية") is its first consumer, so digits follow locale per the "numbers via lib helpers, never `String(value)`" invariant. `date.ts`/`currency.ts` still deferred to their first consumers.
- **Routes** `routes/brands/index.tsx` + `routes/brands/[id].tsx` (bracket filename matches the architecture map; resolves fine under Vite). `ROUTES.BRAND_DETAIL = "/brands/:id"` + `brandPath(id)` builder (the app's first parametrized route). Both wrapped in `ProtectedRoute`. Temp dashboard link to `/brands` added alongside the Settings affordance (until Feature 14 nav).
- **No `logActivity`** this feature (explicit — the first real call, `deal_created`, lands in Feature 10). Detail page's Deals section + the directory deal count are placeholder shells (em-dash / "comes with deals" copy), no `ad_deals` query.
- Verified: typecheck + lint + production build all clean (bundle ~611 kB — the >500 kB warning is the pre-existing code-split deferral to Feature 17, not this change). **Live gate pending (developer-gated):** apply `0004`, create + edit a brand from the running app, then the 2nd-user RLS test on `brands`. **Not yet LIVE-eyeballed** — 375px + RTL pass on the directory, the Add/Edit sheet, and the detail page.
- **`/review` fixes applied (developer said "fix"):** (1) `BottomSheet` now **returns focus to the trigger on close** (captured before focus moves in) — a11y for the reusable overlay; (2) the directory row now shows `contact_name` on a muted third line (build-plan listed "name (ar+en), contact"). Minors left deferred per the review (English "1 brands" pluralization, non-UUID `:id` → load-error vs not-found, no add affordance while the list query errors, `aria-modal` background-inert refinement, long-email tooltip).

**Feature 10 — Ad Deals Page (architect-planned; static-verified):**
- **Mutation surface = create + deliverable toggle + cancel** (developer-approved). Field-editing (title/amount/deadline) deferred — those fields interact with payments (F11/12); deferring designs the edit flow once, when payments exist.
- **Checklist toggles BOTH ways** (developer-approved): unmark clears `posted_at` and the status machine recomputes in both directions (posted → in_progress → pending mirrors the forward path). Read-only guards: `cancelled` and `paid` lock the checklist (paid unreachable until F11 but encoded now in `isChecklistLocked` + the UI). Activity logs fire on FORWARD transitions only (`deliverable_posted`, `deal_posted`); unmarking logs nothing (no "unposted" kind in the closed 7-kind set); a re-mark after unmark logs again — accepted as honest history.
- **Status machine** `features/deals/status.ts` — pure, React/Supabase-free: `deriveDealStatus(deliverables, current)` (terminal paid/cancelled pass through untouched), `getDeliverableProgress`, `toggleDeliverable` (immutable), `becamePosted`, `isChecklistLocked`. Status is written ONLY from `useToggleDeliverable` via this module — never ad-hoc.
- **Migration `0005_ad_deals.sql`** — architecture's exact columns; `user_id default auth.uid()`; status CHECK = the closed 5-status set; verbatim 4-policy RLS + grants/`revoke anon`; index `(user_id, status, deadline)`. **`brand_id` is `on delete restrict`** (not specified by architecture — chosen so a brand with deal/money history can't be deleted out from under it; there's no delete-brand UI anyway). **`updated_at` set in app code** on every write (no triggers, per project stance). `deliverables` is `not null default '[]'::jsonb`; the strict shape is enforced by `dealSchema` at the write layer (one checklist line = type × count, one checkbox posts the whole line).
- **Filters:** status = scrollable segmented chips per ui-rules (See all · In progress · Pending · Posted · Paid — **no Cancelled chip**, cancelled surfaces under "See all"; ui-rules' chip row deliberately wins over build-plan's "status dropdown"); brand + month = new native `Select` primitive; month filters by **deadline** month, options derived from the index query (only months that have deals), labelled Gregorian-only (`formatMonthYear` — Hijri months don't align with Gregorian buckets). Filters live in the DB query + queryKey.
- **Query topology — three queries under the `QUERY_KEYS.DEALS` prefix:** `useDeals(filters)` (`[deals, "list", filters]`, DB-filtered, deadline asc `nullsFirst: false` + created_at desc tiebreak), `useDealsIndex()` (`[deals, "index"]`, lightweight `brand_id, deadline` select → directory counts grouped client-side + month options; the no-N+1 query), `useDealsForBrand(brandId)` (`[deals, "brand", id]`). Every mutation invalidates the prefix once.
- **Money rollup over the FILTERED result** ("{n} posted · SAR x pending"): done = status posted|paid; pending SAR = sum of `agreed_amount_sar` over pending|in_progress. Filtering by brand makes it that brand's rollup — intended reading.
- **First real `logActivity` calls** (from mutation hooks, never components): `deal_created` (summary = deal title), `deliverable_posted` (summary = `title · type`), `deal_posted` (summary = title); `refId` = deal id, `refTable` = `ad_deals`; invoked as `void logActivity(...)` (fire-and-forget; the helper never throws). Cancel logs nothing (no kind for it in v1).
- **lib/date.ts + lib/currency.ts pulled forward (closing Feature 03):** `formatDualDate` forces calendars explicitly (`-u-ca-islamic-umalqura` / `-u-ca-gregory` — ar-SA's default calendar has flipped across CLDR versions) and pins `timeZone: "UTC"` (a YYYY-MM-DD deadline parses as UTC midnight; local-tz formatting could shift a day). `formatSar` uses `currencyDisplay: "code"` ("SAR 19,000" per the mocks), min 0 / max 2 fraction digits; Intl owns digits + placement per locale — **placement in ar to be eyeballed at the live pass**.
- **Add-deal sheet with no brands yet** shows a "deal belongs to a brand" notice + link to /brands instead of an unfillable form. Deal rows have **no leading stripe** (the status pill carries state; deal rows aren't in ui-tokens' stripe table — matches BrandListItem). **Deal notes display in the expanded row** — a small addition beyond build-plan's checklist+placeholders list, included so captured notes aren't invisible until some later feature.
- **Cancel = two-step inline confirm** in the expanded row (destructive Button → confirm/keep pair) — no new modal primitive. Blocked on paid deals.
- Verified: typecheck + lint + production build all clean (bundle ~644 kB — the >500 kB warning is the pre-existing code-split deferral to Feature 17). **Live gate pending (developer-gated):** apply `0005`, create a deal, toggle deliverables (status advances and regresses), confirm the three `activity_log` row kinds, cancel a deal, 2nd-user RLS test on `ad_deals`. **Not yet LIVE-eyeballed:** 375px + RTL pass on the list, filters (chips scroll), expanded row, and Add sheet; Arabic SAR placement.

**Feature 11 — Atomic Mark-Received (SCHEMA-FIRST; architect-planned; static-verified):**
- **Build order honored:** RPC + table + atomicity test BEFORE any UI (build-plan's named exception to UI-first). The UI landed only after the test script existed.
- **Migration `0006_payments.sql`** — architecture's exact columns; status CHECK (`pending`/`received`/`overdue`), method CHECK (`bank`/`cash`/`other`); verbatim 4-policy own-row RLS + grants/`revoke anon`; index `(user_id, status, expected_date)`. **`deal_id` is `on delete restrict`** (mirrors `ad_deals.brand_id` — money history protects its parent). `user_id default auth.uid()`.
- **Migration `0007_mark_payment_received_rpc.sql`** — `security invoker`, ownership via explicit `user_id = auth.uid()` predicates. **Returns jsonb** `{ payment_id, deal_id, amount_sar, deal_title, deal_paid }` (developer-approved deviation from the architecture sketch's `returns void` — the edge function needs to know whether to log `deal_paid`, and `returns table` would collide the output column names with the input parameter). **Guards:** already-received payments are rejected (no double-mark); **cancelled is terminal** (developer-approved) — money arriving on a cancelled deal is recorded on the payment but the deal-update step has `status <> 'cancelled'`, so a cancelled deal never flips to paid. `revoke from public/anon` + `grant execute to authenticated`.
- **Edge `_shared/` scaffolding landed** (the template every later function copies): `api.ts` (ok/fail envelope + **CORS headers + preflight** — the architecture template omits CORS but browser `functions.invoke` fails without it), `constants.ts` (HTTP + ERROR_CODE), `supabase-server.ts` (`createSupabaseAsUser` default + `createSupabaseAdmin`), `logger.ts` (the sanctioned Deno console chokepoint), `logActivity.ts` (never throws; under the as-user client `user_id default auth.uid()` fills — a future service-role caller MUST pass `userId` explicitly), plus `backend/config/env.ts` (validated Deno env; no inline `Deno.env.get` elsewhere).
- **Edge function `mark-payment-received/index.ts`** — template-exact: OPTIONS preflight, try/catch, zod `{ paymentId: uuid }`, RPC call, maps the RPC's "payment not found" raise to 404 (missing / not-owned / already-received are deliberately indistinguishable — no cross-tenant probing), logs `payment_received` always (summary `title · SAR amount`) + `deal_paid` when flagged (summary title), returns the envelope. zod via `npm:zod@3`; supabase via `jsr:@supabase/supabase-js@2`.
- **Atomicity test `backend/supabase/tests/test_mark_payment_received.sql`** — the ACCEPTANCE GATE, developer-run in the SQL editor, repeatable (one transaction, ends ROLLBACK). Simulates the signed-in user via `set_config('request.jwt.claims', …)` (SQL editor runs as table owner → RLS bypassed there; ownership flows through the RPC's explicit predicates — the 2nd-user RLS test still happens live). 5 phases: partial payment leaves the deal untouched → **fault injection** (temp `NOT VALID` CHECK rejecting `paid`; asserts NEITHER row changed) → happy path (both flip together, `deal_paid=true`) → cancelled-terminal → double-mark rejected.
- **Frontend:** `features/payments/payment.schema.ts` (all-string RHF fields, same SAR rules as the deal form; method/expectedDate optional `""`→null at the hook); `features/payments/overdue.ts` (**overdue is DERIVED, never stored, in v1** — pending + `expected_date` < today; `lib/date.ts` gained `todayIsoLocal()`, deliberately not UTC `toISOString` which is wrong for part of every Saudi day); `hooks/usePayments.ts` — `usePayments(tab)` (Pending = `neq received` sorted expected_date asc nulls-last; Received = received_date desc), `useCreatePayment` (plain insert — only mark-received needs atomicity), `useMarkPaymentReceived` (**the ONLY payment-status write path**: edge function → RPC; **optimistic removal from the Pending list with snapshot rollback**; invalidates PAYMENTS + DEALS). `QUERY_KEYS.PAYMENTS` + `constants/payments.ts` (`PAYMENTS_TAB`, `EDGE_FUNCTION` slugs).
- **UI:** `routes/payments.tsx` wired in `App.tsx` (protected) + temp dashboard link. `FilterChips` as the Pending/Received tabs; **`TotalPendingStrip`** (ui-rules' one sanctioned colored callout) on the pending tab; `PaymentListItem` (row-card **stripes from the token table**: pending=warning, derived-overdue=error, received=none), `PaymentStatusPill` (payments token table; **label-only — the payments pill table defines no dot**), `PaymentForm` in a `BottomSheet` (deal select lists **active deals only** — pending/in_progress/posted, per Decision 3; no over-payment enforcement v1). Deal titles resolved from the cached `useDeals({})` query (no join, no N+1). No-active-deals case: sheet shows a "payment belongs to a deal" notice + /deals link.
- **Deferred:** "Send reminder" button (F12 gates it; reminders table is F13); `usePaymentsForDeal` + deal-row payment status (F12); field-editing of payments (with deals', when the edit flow is designed once).
- Verified: typecheck + lint + production build all clean (bundle ~657 kB — the >500 kB warning is the pre-existing code-split deferral to F17). **Live gate (developer-gated, THE acceptance gate):** apply `0006`+`0007`, run the atomicity test (ALL PHASES PASSED), deploy the function, exercise add-payment → mark-received → deal flips paid, confirm activity rows, 2nd-user RLS on `payments`. **Not yet LIVE-eyeballed:** 375px + RTL pass on /payments.
- **`/review` run; Important fix applied (developer said fix):** the RPC now takes the deal's **row lock (`for update`) before the all-received check** — without it, two final installments marked concurrently could each snapshot the other's payment as still pending, both skip the paid flip, and the deal would sit fully received but never paid with nothing left to re-trigger it. Lock order is payment row → deal row in every caller (no deadlock cycle). Edited `0007` in place (it was not yet applied). **Minors left deferred per the review:** envelope `ok()/fail()` lives Deno-side in `functions/_shared/api.ts` (not `backend/shared/api.ts` per the architecture map — jsr imports can't be shared with Vite) with the `Envelope` type mirrored locally in `usePayments.ts`; amount/date validation patterns duplicated between deal/payment schemas (extract on the third copy, F13); hook's `!envelope.ok` branch is dead for non-2xx (FunctionsHttpError surfaces first — toast is localized anyway); received-row null-date fallback wording; mark-received button variant (secondary vs primary) pending a /payments mock; test script assumes ≥1 auth.users row. Also explicitly approved post-hoc: the RPC's double-mark guard (`status <> 'received'`, test phase 5).

**Feature 12 — Payment ↔ Deal ↔ Dashboard Wiring (no `/architect` — no new boundary, no migration, no edge function; static-verified):**
- **Scope honored exactly per build-plan:** `usePaymentsForDeal` + the deal-row payment status line + the gated Send-reminder button. The deals list already reflects `paid` via the status pill (free from F11); dashboard rollups stay F14.
- **`usePaymentsForDeal(dealId)`** in `hooks/usePayments.ts` — keys off `[...PAYMENTS, "deal", dealId]`, so the existing create/mark-received invalidations of the `PAYMENTS` prefix already refresh it (and mark-received also invalidates `DEALS`, refreshing the row's status pill). Returns the query spread plus a derived `summary` (build-plan: "payments + a derived isFullyPaid flag"). Ordering mirrors the pending tab (expected_date asc nulls-last, created_at desc tiebreak).
- **Pure derivation in `features/payments/summary.ts`** — `getPaymentsSummary(payments)` → `{ receivedCount, totalCount, outstandingSar, isFullyPaid }`. Outstanding sums every NON-received installment (robust to a stored 'overdue', mirroring `neq received`). `isFullyPaid` is false for zero payments — a deal with no installments isn't "fully paid".
- **No-N+1 guard:** the query lives in `DealExpandedPanel`, which `DealListItem` only mounts when a row is EXPANDED — the deals list never fans out into per-row payment queries; each expanded deal is one on-demand, cached query.
- **Payment status line states:** loading = small skeleton bar; error = inline `text-error-foreground` line; empty = muted "No payments for this deal yet"; populated = `deals.expanded.paymentsSummary` ("X of Y payments received · SAR Z outstanding") with counts via `formatNumber`, money via `formatSar` + `.money` class. The F12 placeholder string was removed from both catalogs; the F15 snap placeholder stays.
- **"Send reminder" gating = info toast, not a hover tooltip** (decision): the build-plan says "gated behind a 'Coming with feature 13' tooltip", but hover tooltips don't exist on a 375px touch app and a disabled button swallows events. The button renders in its FINAL spot (ui-rules: Mark received + Send reminder side-by-side, equal width `flex-1`, primary action on the leading edge; secondary + ghost pair matching the cancel-confirm precedent), and tapping it shows `payments.reminderComingSoon` as an **info toast**; `title` carries the same string for pointer hover. User-facing copy says "coming soon" — feature numbers are developer-speak. F13 swaps the handler for `createReminder({ kind: 'payment' })` — zero layout change.
- **Toast gained a third variant `info`** (`lib/toastContext.ts` + `Toast.tsx`): `Info` icon + `text-info-foreground`, tokens already in the table. Additive; existing success/error call sites untouched.
- Verified: typecheck + lint + production build all clean (bundle ~660 kB — the >500 kB warning is the pre-existing code-split deferral to F17). **Live verification rides the F11 gate** (needs `0006`/`0007` applied + the edge function deployed): expand a deal with installments → line shows real counts; mark the last installment received → expanded row line and status pill update together (mark-received invalidates PAYMENTS + DEALS). **Not yet LIVE-eyeballed:** 375px + RTL on the expanded payment line and the payments-row button pair.
- **`/review` run; all 4 minors fixed (developer said fix):** (1) `isFullyPaid` now has a consumer — a fully-paid deal's expanded row shows a distinct success-toned "All payments received" line (`text-success-foreground`; green = money realized, matching the collection-rate convention) instead of "N of N · SAR 0 outstanding"; (2) the tooltip→info-toast deviation stands as resolved-by-decision (touch app; intent met); (3) `usePaymentsForDeal` no longer spreads the query result — a module-stable `select` (`selectPaymentsForDeal`) exposes `{ payments, summary }` as `data`, keeping TanStack v5 tracked-query render optimization and the "hooks return useQuery" pattern; (4) `paymentsSummary` now uses i18next JSON-v4 plural suffixes — `_one`/`_other` in en, the full `_zero/_one/_two/_few/_many/_other` set in ar with correct number–noun agreement (دفعة/دفعتين/دفعات), `count` passed alongside the locale-formatted display values; the ar outstanding clause rephrased to "المتبقي {{outstanding}}" to sidestep gender agreement. The PRE-EXISTING naive-plural keys (deals.count, payments.count, brands.count/dealCount — the deferred F09 minor) are untouched; a project-wide pluralization pass remains a F17-polish candidate.

**Feature 13 — Meetings + Reminders (architect-planned; static-verified):**
- **Mutation surface = create + full-field edit + cancel** (developer-approved). One `MeetingForm` bottom sheet reused create/edit (F09 pattern); tapping a meeting row opens the edit sheet; "Cancel meeting" is a destructive two-step confirm INSIDE the edit sheet. **'done' ships in the schema CHECK but no UI writes it** — deferred until something reads it. No delete (cancel is terminal, mirrors deals).
- **Migration `0008_meetings_reminders.sql`** — both tables together (the architecture map pairs them): architecture's exact columns, verbatim 4-policy RLS + grants/`revoke anon` each, `meetings (user_id, scheduled_at)` + `reminders (user_id, due_at, is_done)` indexes. **`meetings.brand_id`/`deal_id` are `on delete set null`** (nullable convenience links, not money history — the RESTRICT precedent is money-specific). `reminders.ref_id` is text with NO hard FK (architecture schema) so a reminder never blocks/cascades its source row.
- **`lib/createReminder.ts` = THE reminder write path** (build-plan's "ONE helper"), the `logActivity`-style sanctioned non-hook writer — but it THROWS (a missing reminder is user-visible Today-panel state, not telemetry). **Upsert-by-ref semantics:** an open reminder for (kind, ref_id) is MOVED (due_at + bilingual messages refreshed, re-armed `is_done=false`) instead of duplicated — one behavior covers meeting create (insert), meeting edit (move + self-heal if the create-time write failed), and payment double-tap (no stacking). `deleteReminderForRef` is the cancel path.
- **Reminder lifecycle decisions:** cancel DELETES the reminder (derived scheduling state, not history; `is_done=true` would mean "user handled it"). Write order guards strays: create = meeting first (a reminder never points at nothing); cancel = reminder delete FIRST, then status flip (a failure aborts the whole cancel — never a cancelled meeting still feeding Today). **Two-step non-atomicity is deliberate** (reminders in app code, never triggers): if the meeting saves but the reminder write fails, the mutation resolves with `reminderFailed: true` and the route shows a distinct "saved, but reminder couldn't be set" error toast — no silent missing reminder, no duplicate meeting on retry (editing the meeting re-creates it via the upsert).
- **Payment reminder `due_at` = `expected_date`** at start of the viewer's LOCAL day (todayIsoLocal convention), falling back to NOW when null or past (decision 3). Lead-minutes stays meetings-only. Meeting reminder `due_at = scheduled_at − reminder_lead_minutes` (current value from the cached `useAppUser`, default 60 fallback); edit recomputes from the CURRENT lead ("affects future reminders only" is literal).
- **Bilingual messages denormalized** (`message_en` + `message_ar` both always written, same stance as `activity_log.summary`) via pure builders in `features/reminders/messages.ts`; callers pass pre-formatted SAR strings (`formatSar` per locale) so features/ stays free of lib formatting imports.
- **Calendar = Gregorian month grid** (Hijri months don't align with its buckets — same rationale as the deals month filter; dual dates stay on headings/rows via `lib/date.ts`). **Native Date math in `features/meetings/calendar.ts`** — the planned date-fns install was SKIPPED (deviation from the approved plan's assumption): ~15 lines of native arithmetic sufficed, honoring the "simpler native solution" dependency rule; date-fns stays deferred to its first genuinely awkward consumer. Week starts SUNDAY (Saudi week). All grid/bucketing math runs in the viewer's LOCAL timezone (a 00:30 Riyadh meeting belongs to that local day, not its UTC day).
- **Query topology:** `useMeetingsForMonth(month)` keys off `[...MEETINGS, "month", "YYYY-MM"]` — ONE local-month range query feeds both list and grid; mutations invalidate the `MEETINGS` + `REMINDERS` prefixes (reminders have no reader until F14 — invalidated now so the Today panel is always fresh). **Cancelled meetings are excluded from the month query** (small in-build decision: terminal, no v1 consumer; mirrors how deals tuck cancelled away).
- **`lib/date.ts` gained** `formatDualTimestampDate` (dual date in the viewer's LOCAL tz — formatDualDate pins UTC, correct for plain dates but wrong for timestamps near midnight), `formatTime` (locale digits + 12/24h via Intl), `weekdayLabels` (grid header), `toDatetimeLocalValue` (ISO → `<input type="datetime-local">` value, inverse of the write layer's `new Date(value).toISOString()`).
- **F12's Send-reminder button is LIVE:** `useSendPaymentReminder` in `usePayments.ts`; `payments.reminderComingSoon` removed from both catalogs (replaced by `payments.toast.reminderSet`/`reminderError`); `PaymentListItem` gained per-row `isSendingReminder` and lost the tooltip `title`. Meeting links in the form use the same ACTIVE-deal set as the payment form (second copy — extract to constants on the third).
- **i18n:** full `meetings.*` tree en + ar; `meetings.count` uses JSON-v4 plural suffixes (en `_one/_other`; ar full `_zero/_one/_two/_few/_many/_other` with اجتماع/اجتماعان/اجتماعات agreement), `count` + formatted `total` passed together (F12 precedent).
- **`/review` run; all 5 findings fixed (developer said fix):** (1) the calendar is `role="group"` + `aria-label`, NOT `role="grid"` (a half-faked ARIA grid without row/gridcell + arrow-key nav reads as broken to screen readers); (2) `createReminder`/`deleteReminderForRef` now take `userId` and carry the `user_id` convenience filter on every statement (code-standards convention; RLS stays the real check) — callers pass `meeting.user_id`/`payment.user_id`/session id; (3) the attendees `Label` sets `htmlFor` only when an attendee row exists (create mode starts at zero rows — no dangling reference); (4) `resolveLeadMinutes` replaces the silent 60-minute fallback: cache hit → cached value, cache miss → direct one-shot `app_users` select, and the bootstrap default only when even that fails (where the reminder write would fail anyway → `reminderFailed` toast); (5) `0008` gained a **partial unique index `reminders (user_id, kind, ref_id) where ref_id is not null`** — DB backstop for the upsert's check-then-insert race (edited in place; migration not yet applied). **Accepted as-is:** edit re-arms a dismissed reminder (the agreed "move" semantics — revisit when F14 builds dismissal); the Toast `info` variant currently has no consumer (generic primitive); the three documented deviations stand (date-fns skipped for native Date; cancelled meetings excluded from the month query; meeting deal-links limited to ACTIVE deals).
- Verified: typecheck + lint + production build all clean (bundle ~686 kB — the >500 kB warning is the pre-existing code-split deferral to F17). **Live gate pending (developer-gated):** apply `0008`, then from the app: create a meeting → `reminders` row with `due_at = scheduled_at − lead`; edit its time → reminder moves; cancel → reminder row gone, meeting row stays `cancelled`; tap Send reminder twice on a pending payment → ONE `kind='payment'` row; confirm the `meeting_scheduled` activity row; 2nd-user RLS test on `meetings` + `reminders`. **Not yet LIVE-eyeballed:** 375px + RTL pass on the meetings list, calendar grid (RTL column flow), edit sheet, and the datetime-local input in Arabic.

**Feature 14 — Dashboard (architect-planned; static-verified):**
- **Definitions locked in the plan:** "this month" = the VIEWER'S LOCAL calendar month (client computes `[month_start, month_end)` and passes it to the RPC — Postgres `now()` is UTC and would misbucket); **invoiced** = sum `agreed_amount_sar` over non-cancelled deals with `deadline` in month (deals-month-filter bucketing; null-deadline deals are in no month); **collected** = received payments by `received_date` in month; **outstanding** = ALL non-received installments all-time (the Payments-tab convention — deliberately ≠ invoiced − collected); **posted/pending** = month-deal counts, posted|paid vs pending|in_progress, cancelled in neither.
- **Migration `0009_dashboard_stats.sql`** — `get_dashboard_stats(month_start date, month_end date) returns jsonb`, `language sql, stable, security invoker, set search_path = public`, explicit `auth.uid()` predicates on every subquery; `revoke from public/anon` + `grant execute to authenticated`. Called straight from the browser via `supabase.rpc` (read-only → no edge function). NOT yet applied — see the 14 live gate.
- **Hooks:** `useDashboardStats(month)` (`[...DASHBOARD, "stats", month]`; `QUERY_KEYS.DASHBOARD` added); `useTodayMeetings` + `useTodayReminders` (two direct queries keyed `[...MEETINGS,"today"]` / `[...REMINDERS,"today"]` so every existing meeting/reminder invalidation refreshes them for free — window computed at FETCH time, not render time, so the key never goes stale; merged in the panel via pure `mergeTodayItems`); `useNeedsAttention` exports `useOverduePayments` (`[...PAYMENTS,"attention"]`, status=pending + `expected_date < todayIsoLocal()` — the derived-overdue rule, filtered in the DB) + `usePastDeadlineDeals` (`[...DEALS,"attention"]`, pending|in_progress + `deadline <` today); `useReminders.useDismissReminder` (`is_done=true`). Deal mutations (create/toggle/cancel) + payment mutations (create/mark-received) now ALSO invalidate `DASHBOARD`.
- **Reminder dismissal decided (the F13 open question):** Done on a Today reminder row sets `is_done=true` ("user handled it") — distinct from F13's cancel path which DELETES (a cancelled source has nothing to handle). Accepted: editing a meeting re-arms a dismissed reminder (upsert-by-ref move semantics — a moved meeting is a new thing to be reminded of).
- **Today merge rules** (pure `features/dashboard/todayItems.ts`): meetings in `[now, +24h)` + open reminders `due_at < now+24h` (past-due undismissed reminders stay visible — they don't silently vanish); **dedupe** drops a `kind='meeting'` reminder whose meeting is already in the window (else every near meeting shows twice), but keeps it when the meeting lies beyond the window (it's the only signal today); sorted by due time. Overdue rows swap the time pill for an "Overdue" error pill (a stale clock time would mislead).
- **Hero composition:** invoiced is the headline metric; ring = collected ÷ invoiced via pure `collectionRate` (null → em dash, never NaN; clamped to 1 — early-month collections of old installments can exceed the month's invoiced); posted/pending ride a label-value hero line (`"Posted {{posted}} · Pending {{pending}}"` — label+number phrasing dodges Arabic number–noun agreement, so no plural keys needed); tiles = Collected/Outstanding/Posted exactly per the ui-tokens tile table. Sparkline deferred to F16 (needs monthly history + recharts). `CompletionRing` gained an additive `tone="onAccent"` for the white-on-violet ring.
- **Brand-detail rollups** (pure `features/brands/rollup.ts`, client-side over the page's ONE `useDealsForBrand` query): lifetime total + avg deal size over non-cancelled deals; last engagement = most recent deal's `created_at` (any status — a cancelled deal is still engagement). `PlaceholderStat` → `RollupStat` (em dash when nothing to roll up).
- **Out of scope, explicitly:** bottom tab bar / FAB / profile menu (no feature number owns nav — its own slice; quick-links row survives at the dashboard's bottom); the recent-activity feed (parked on the bilingual `activity_log.summary` rendering question); stored month history.
- Verified: typecheck + lint + production build all clean (bundle ~700 kB — the >500 kB warning is the pre-existing code-split deferral to F17). **Live gate** in Current Status. **Not yet LIVE-eyeballed:** dashboard at 375px + RTL (hero gradient + ring, tiles, Today rows incl. Done button and Arabic reminder messages, attention rows), brand-detail stat row.
- **Review fixes applied after F14 (developer said "fix it"):** (1) reminder rows with actionable refs now navigate: payment/payment-ref → `/payments`, deliverable/ad_deals → `/deals`, meeting/meetings → `/meetings`; the Done button stops propagation so it dismisses instead of navigating; custom unlinked reminders remain static; (2) next-day items inside the 24h window show a localized tomorrow-time pill (`Tomorrow, {{time}}` / `غدًا، {{time}}`) instead of a bare clock time; (3) `useTodayItems` was split into `useTodayMeetings` and `useTodayReminders` so each remains a direct TanStack query, with `mergeTodayItems` composed in the panel; (4) Today and Needs-attention now degrade on partial failure by showing the loaded half plus a localized error Card for the failed half; (5) Today owns clock state that ticks at the next due minute/due timestamp, removing the render-time `new Date()` overdue check; (6) reminder dismissal pending UI is tracked per row with a local `Set`, so concurrent Done taps do not hand the disabled/spinner state from one row to another. Verified again: typecheck + lint + production build clean. Browser smoke was limited: dev server started after sandbox approval and `/dashboard` redirected to `/login` as expected without an initial crash, but follow-up `agent-browser` snapshot/eval commands were blocked by restricted npm registry access.

**Feature 15 — Snap Extraction (SCHEMA-FIRST; architect-planned; static-verified incl. `deno check`):**
- **Build order honored:** migration → edge function → UI (build-plan's second named exception to UI-first; the highest-risk feature — uploads + paid API + untrusted image content).
- **Migration `0010_snap_reports.sql`** — architecture's exact columns (all six extracted fields nullable ON PURPOSE: Snap UI variants omit numbers, the model returns null rather than inventing); `user_id default auth.uid()`; status CHECK (pending/extracted/failed/manual); verbatim 4-policy RLS + grants/`revoke anon`; indexes `(user_id, created_at desc)` (history list + the rate-limit count) and `(user_id, deal_id)` (deal expanded line); **`deal_id` on delete set null** (convenience link — the meetings precedent, not money-RESTRICT); `alter publication supabase_realtime add table snap_reports` (realtime requires it; RLS still gates events); **PRIVATE `snap-uploads` bucket** with own-path select/insert/delete (architecture's stated stance — unlike avatars' public-read deviation; screenshots are business data). `source_file_url` stores the bucket-relative PATH, never a URL.
- **Edge function `extract-snap-report`** — template-exact. Input `{ snapReportId }` ONLY; the path comes from the row loaded as-user (no client URL → no SSRF/IDOR). Guards in cost order: missing/not-owned → 404 (indistinguishable); not-pending → **409 CONFLICT** (new `ERROR_CODE.CONFLICT`; re-invoking a row can't double-spend or clobber manual edits — also why there's NO retry button: failed → manual entry, fresh attempt = new upload); per-user hourly count vs `ENV.SNAP_RATE_LIMIT_PER_HOUR` (default 20) → 429; then as-user storage download → **OpenAI GPT-4o via plain `fetch`** (resolved-by-decision deviation from the approved-deps `openai` entry — one POST + zod re-validation beats an npm dep in Deno, per the "simpler native solution" rule) with `response_format: json_schema` **strict, all six keys required but nullable**, temperature 0, and a bilingual system prompt (Arabic/English label maps, Arabic-Indic digit + ألف/K/M conversion, **image text is data never instructions**). Non-ISO `snap_date` → null (manual fix) rather than a failed write. Success update is guarded `eq(status,'pending')`; failures flip the row `failed` (same guard) + store `raw_ai_json`; `logActivity('snap_extracted')` after success.
- **`backend/config/env.ts`:** `OPENAI_API_KEY` is a **lazy getter** — every function imports ENV via supabase-server.ts, and an eager `required()` would crash `mark-payment-received`'s boot whenever the secret isn't set; the getter still fails loudly at first access. `SNAP_RATE_LIMIT_PER_HOUR` is optional-with-default (tunable, not a secret).
- **Upload hardening (the deferred F06 magic-byte item lands here):** `features/snap/upload.ts` validates MIME **and** leading bytes (PNG/JPEG/WebP/PDF signatures) **and** size (5 MB image / 10 MB PDF) as pure functions; `lib/pdfToImage.ts` renders a PDF's FIRST page to PNG at 1280px via **dynamically-imported pdfjs-dist 6.0.227** (own ~472 kB lazy chunk + worker asset — never in the main bundle; v6 renders into `canvas` directly, `destroy()` lives on the loading task). Only images ever reach the bucket.
- **Hooks `useSnapReports.ts`:** list (`[...SNAP_REPORTS,"list"]`), `useSnapReportsForDeal` (`[...SNAP_REPORTS,"deal",id]`, mounted on expand only — the F12 no-N+1 pattern), `useCreateSnapReport` (upload → insert pending row → invoke; **an invoke failure is NOT a mutation failure** — the row is marked `failed` client-side (guarded on pending) and the mutation resolves with `extractionQueued:false` so the route picks the toast; nothing ever sits pending forever), `useUpdateSnapFields` (manual override → `'manual'`, never reverts), `useLinkSnapToDeal` (deal_id only — linking is metadata, status untouched), `useSnapSignedUrl` (60-min signed URL, 45-min staleTime), `useSnapReportsRealtime(enabled)` (ONE `postgres_changes` UPDATE channel per user filtered `user_id=eq.`, enabled only while something is pending; invalidates the prefix on event — list, deal lines, and the open sheet all refresh).
- **UI:** `routes/analytics/snap.tsx` (wired in App.tsx + a quick-links entry on the dashboard): upload Card (`SnapDropzone`) + history list (`SnapReportListItem`) + detail `BottomSheet` (`SnapReportSheet` — the sheet re-derives its report from the cached list so a realtime settle refreshes it live while open). Failed reports open the sheet with ALL fields editable (manual entry IS the recovery). Link select offers **non-cancelled** deals (paid deals are exactly the campaigns brands want numbers for). Deal expanded row's F10 placeholder replaced by the read-only snap line (date · views, em dashes, `<Link>` to /analytics/snap). New "Status Pills — Snap reports" table added to ui-tokens.md (existing families, no new hex).
- **i18n:** full `snap.*` tree en+ar; `snap.count` uses JSON-v4 plural suffixes (en `_one/_other`, ar full six-form set with تقرير/تقريران/تقارير agreement); `deals.expanded.snapPlaceholder` replaced by `snapReport/snapSummary/noSnapReport/snapError` in both catalogs.
- Verified: typecheck + lint + production build clean (main bundle ~727 kB — the >500 kB warning remains the F17 code-split deferral; pdf.js is already split out), **plus `deno check` on the edge function** (a first — deno was available this session). **Live gate (developer-gated)** in Current Status. **Not yet LIVE-eyeballed:** /analytics/snap + the deal snap line at 375px + RTL.

**Feature 16 — Reports — Monthly & Per-Brand (UI-FIRST on mock data; no `/architect` — no new boundary, no migration, no edge function; static-verified):**
- **Scope honored exactly per build-plan: UI on mock data this session; the aggregate hooks are next.** The route owns two clearly-commented mock arrays (`MOCK_MONTHLY`: 12 months; `MOCK_PER_BRAND`: 4 brands) whose shapes already equal the planned hooks' return types, so wiring is a swap. NO hooks, NO query key, NO migration written.
- **`recharts` installed here (its first + only consumer, code-standards-approved) — resolved to v3.8.1.** Verified the v3 props used against the installed type defs before writing (the lib moves fast: `ResponsiveContainer` w/h, `XAxis reversed`, `YAxis orientation`, `Bar radius`, the v3 `Tooltip formatter(value,name,…)` signature). v3 enables `accessibilityLayer` by default (keyboard-navigable tooltips).
- **The chart is LAZY-loaded** (`React.lazy` + `Suspense` in the route, named-export adapter `.then(m => ({default: m.MonthlyTotalsChart}))` so no default export is introduced) → recharts lands in its OWN chunk (**~358 kB / 108 kB gzip**), NOT the main bundle (which grew only ~5 kB to ~732 kB). This mirrors the pdf.js lazy-chunk precedent; /reports is a leaf page. Code-splitting the rest stays F17.
- **Build-plan "table" → row-card list** (decision): ui-rules forbids `<table>` and the app is single-column 375px-first. Per-brand renders as a stack of static row cards (`PerBrandReportItem`), each = brand + deal count + invoiced SAR + a `ProgressBar fill="success"` collection-rate bar. The build-plan's "brand × month" matrix is covered across the TWO sections — the monthly chart carries the time axis, the per-brand list carries the brand axis — rather than a literal mobile matrix.
- **Collection-rate guard reused, not re-implemented:** `PerBrandReportItem` imports `features/dashboard/stats.collectionRate` (whose comment already anticipated this) — ONE zero-invoiced guard for both the hero ring and the per-brand bar. A brand with `invoicedSar=0` renders an em dash, never NaN; clamped to 100%. The mock deliberately includes one zero-invoiced brand (Almarai) so the guard is visible in the visual pass, and one month where collected > invoiced so the clamp is too.
- **Chart colors = CSS-var token strings** (`var(--color-accent)` / `--color-success` / `--color-border` / `--color-text-muted`) per ui-tokens "Charts (Recharts)" — recharts paints SVG attributes from real color values, so Tailwind utilities don't apply; this is the sanctioned token form. The textual legend swatches above the chart DO use `bg-accent`/`bg-success` utilities. **RTL-tolerant:** `XAxis reversed` + `YAxis orientation="right"` in Arabic so the chart reads right-to-left.
- **`lib` additions (genuinely needed, routed through the helpers per the numbers/dates-via-lib invariant):** `formatCompactNumber` (numbers.ts — locale compact "20K"/"٢٠ ألف" for the dense Y axis) and `formatMonthShort` (date.ts — Gregorian short month for the X axis; Gregorian-only, same rationale as `formatMonthYear`).
- **No segmented "Reports / Snap Analytics" control** (the ui-tokens/ui-rules Insights tab grouping) — that belongs to the still-unowned nav slice. /reports is reached via the temp dashboard quick-link for now (the snap precedent). New `components/reports/` folder; `features/reports/report.ts` holds the two shared types only.
- **Per-brand deal count stays NON-plural** (`reports.perBrand.summary` "{{deals}} deals · {{amount}} invoiced") — mirrors the directly-analogous `brands.dealCount`/`deals.count` keys, not the standalone plural count strings; it's a compound summary line, not a bare count.
- Verified: typecheck + lint + production build all clean (main ~732 kB + the lazy recharts chunk ~358 kB; the >500 kB warning is still the pre-existing F17 main-bundle deferral). **Not yet LIVE-eyeballed:** /reports at 375px + RTL — the bar chart (axis mirroring in Arabic, 12-group density on a phone, tooltip), the per-brand rows + success collection-rate bars + em-dash brand, both empty states; Arabic SAR placement + Arabic-Indic digits on the axes.

**Feature 16 — Reports LOGIC (architect-planned, developer-approved; static-verified) + folded-in deferred F14 hero sparkline:**
- **Aggregation = Postgres RPC** (developer-approved over client-side), honoring the no-N+1 invariant + the `get_dashboard_stats` (0009) precedent — this is the new boundary that warranted `/architect`. **Migration `0011_report_aggregates.sql`** = two read-only `security invoker stable set search_path=public` functions returning `jsonb`, with the 0009 `revoke public/anon` + `grant authenticated` block: `get_monthly_totals(window_start date, window_end date)` and `get_per_brand_report()`.
- **`get_monthly_totals`** — `generate_series` over the [window_start, window_end) month range LEFT-JOINed to two groupings (invoiced = non-cancelled deals by `date_trunc('month', deadline)`, collected = received payments by `date_trunc('month', received_date)`), so it ALWAYS returns 12 ordered rows with empty months as 0 (the chart's "12 bars" contract). The CALLER passes the window as LOCAL first-of-month dates (only the client knows its timezone — same discipline as 0009). Definitions are verbatim-consistent with `get_dashboard_stats` so Reports and Dashboard never disagree.
- **`get_per_brand_report`** — **ALL-TIME per brand** (developer-approved over a 12-month window; matches brand-detail "lifetime total"). Two CTEs: `deal_agg` (count + sum agreed over non-cancelled deals, grouped by brand_id) LEFT-JOINed to `collected_agg` (sum received payments on the brand's **non-cancelled** deals — kept on the SAME deal set as invoiced so a cancelled deal's stray payment can't inflate the rate). Only brands with ≥1 non-cancelled deal appear; ordered by invoiced desc then deal_count desc.
- **Shared `backend/shared/types/reports.types.ts`** — snake_case RPC row shapes (`MonthlyTotalRow`, `PerBrandReportRow`), the `dashboard.types.ts` precedent; the hooks map them to the camelCase `MonthlyTotal`/`PerBrandRow` UI shapes in `features/reports/report.ts`.
- **`hooks/useReports.ts`** — `useMonthlyTotals()` (computes the local 12-month window via `currentMonth`/`addMonths` from `features/meetings/calendar`, keys off `[...REPORTS,"monthly",month]`, maps to `MonthlyTotal[]`) + `usePerBrandReport()` (RPC keyed `[...REPORTS,"per-brand"]` returning rows by brand_id, **JOINED in a `useMemo` to the cached `useBrands` list** → full `PerBrandRow[]`; returns a merged `{ data, isLoading, isError }` of its two queries). Brand RENAME needs no REPORTS invalidation — the join reads the BRANDS cache, which `useUpdateBrand` already refreshes.
- **`QUERY_KEYS.REPORTS` added**; the deal mutations (create/toggle/cancel) and payment mutations (create/mark-received) now invalidate it alongside `DASHBOARD`, keeping both /reports and the hero sparkline fresh.
- **`routes/reports.tsx` wired** — mocks + `mockBrand` deleted; both hooks consumed; per-section loading skeletons (`ChartSkeleton` reused by the Suspense fallback + the query-loading state; `PerBrandSkeleton` = 3 pulse rows) + error Cards (`reports.loadError`) added; empty states kept (monthly "empty" now means **every** month is genuinely 0, since the RPC always returns 12 rows). The **"Last 12 months" caption moved off the page header onto the monthly section** (per-brand is all-time, so a page-wide caption would mislead) — page header is now title-only.
- **Folded-in deferred F14:** hand-rolled SVG **`components/dashboard/Sparkline.tsx`** (presentational; `polyline`, `stroke=currentColor` inherits the hero's on-accent, `preserveAspectRatio="none"` + `vectorEffect="non-scaling-stroke"`, `reversed` mirrors for RTL, flat mid-line when all-equal, `role="img"` + i18n `dashboard.hero.trend` label). **NOT recharts on purpose** — pulling the 358 kB lazy chart chunk onto the LANDING page for a 12-point line would defeat the lazy-load (the `CompletionRing` hand-rolled-SVG precedent). `MonthTotalsBar` now also calls `useMonthlyTotals()` (shared cache) and renders the invoiced-series sparkline under the posted/pending line — **independent of the stats query** (stays absent until it loads, no skeleton, never blocks the hero) and **hidden until ≥1 month invoiced** (an all-zero flat line reads as noise).
- **No SQL test harness** (developer-approved) — read-only aggregates are lower-risk than the atomic write; static verify + the live hand-calc gate suffice.
- Verified: typecheck + lint + production build all clean. **Bundle guardrail held:** recharts stays its OWN ~358 kB lazy chunk (`MonthlyTotalsChart`), main bundle ~734 kB (≈+2 kB) — the hand-rolled sparkline kept recharts OFF the dashboard/landing bundle. **Live gate (developer-gated):** apply `0011`; in Current Status.

**Navigation slice (the unowned shell — bottom tab bar + FAB Quick Add + profile menu + Insights tabs; developer-approved decisions; UI-only; static-verified):**
- **Not a numbered build-plan feature** — the nav was referenced throughout as "the still-unowned slice recommended before/with 17." Built now (developer: "build nav slice") to the **ui-rules/ui-tokens** nav spec; `context/designs/` doesn't exist, so there was no image to match. No `/architect` skill (UI-only, no new boundary/migration/edge fn, below the team's architect bar) — but a 4-question alignment round resolved the behavioral forks first.
- **Decisions (all developer-picked):** (1) **Minimal sticky avatar header** — `AppLayout` adds only a top-trailing avatar; pages keep their own title rows (no 9-page header rewrite). (2) **Profile menu = BottomSheet** with the off-tab destinations Brands · Payments · Settings + Sign out (reuses the app's overlay pattern; gives everything off the tab bar a persistent home). (3) **Quick Add = navigate + auto-open** each route's existing Add sheet (reuses every create flow; Snap navigates only — its upload card is the add surface). (4) **Insights = reuse both routes + a shared segmented control** (no new `/insights` route).
- **Route tree refactor:** App.tsx collapsed from 9 repetitive `<ProtectedRoute>` element wrappers to ONE layout route `<Route element={<ProtectedLayout/>}>` (= `ProtectedRoute` → `AppLayout` → `Outlet`). Auth gating is now in one place; login/entry/callback stay outside the shell (no chrome on auth screens).
- **`useQuickAddOpen(onOpen)` is effect-based, not mount-seeded** (the key correctness call): navigating to the route you're ALREADY on pushes a new history entry without remounting, so a `useState` seed would miss it. The hook watches `location` and fires `onOpen()` when `location.state.quickAdd` is set, then consumes the signal (`replace`, `state:null`) so a refresh/back-nav doesn't re-pop the sheet. Each Add route passes a `useCallback`-stable opener (stable setters → stable dep).
- **FAB is hand-positioned, not a tab** — `MobileTabBar` renders 4 `<Link>` tabs + a center FAB button (`-mt-7 size-14 shadow-fab`) that calls back to `AppLayout` (which owns the Quick Add sheet state). Active state reads via **color + label weight** (lucide `1.17.0` is outline-only — no filled-glyph variant for ui-tokens' "icon filled"). Icons verified to exist at the pin before use (Home/Megaphone/Calendar/BarChart3/Plus/CalendarPlus/Image/Wallet/Building2/Settings/LogOut/User).
- **Quick Add tile circles reuse existing brand-tint backgrounds + deeper-tone foregrounds** (violet/blue/amber/green) — no new tokens, same discipline as `BrandAvatar`.
- **Bottom clearance** is owned by `AppLayout` (`pb-[calc(env(safe-area-inset-bottom)+88px)]` around the Outlet) so the fixed bar + floating FAB never cover content; pages were left untouched (the "minimal" decision). Known minor cosmetic: pages keep `min-h-dvh`, so short pages allow a little over-scroll into empty space above the bar — acceptable for the slice; a per-page `min-h` cleanup is a future option.
- **i18n:** new `nav.*` (home/deals/calendar/insights/quickAdd/openMenu/menuTitle/barLabel) + `quickAdd.*` (deal/meeting/payment/snap) in en+ar; the profile menu + Insights labels REUSE existing keys (brands/payments/settings/reports/snap titles, settings.actions.signOut).
- Verified: typecheck + lint + production build all clean. Main bundle ~745 kB (≈+10 kB); recharts stays its own 358 kB lazy chunk (Insights reuses `FilterChips`). **Not yet LIVE-eyeballed** — the nav visual gate (375px + RTL: tab bar, FAB Quick Add, profile menu, sticky avatar header, Insights tabs; Quick Add opens each Add sheet incl. from the same page; sign-out → /login) is in Current Status.

**Post-v1 polish (shipped):** the shell header's notification **bell linked to /payments** — later REMOVED (deal-lifecycle follow-up): Payments now has a proper labelled home on the **dashboard** (the Collected/Outstanding stat tiles are `<Link>`s to /payments, Posted → /deals, plus a right-aligned "View payments →" link above the tiles — `MonthTotalsBar`); the header keeps only the presentational search button. `dashboard.viewPayments`/`viewDeals` i18n added; orphaned `nav.notifications` removed; ui-registry updated. **Quick Add gained a Brand tile** (now Brand · Deal · Meeting · Payment · Snap report; Snap spans the last row; Brand leads because a deal requires a brand — `BrandsRoute` wired `useQuickAddOpen`; `quickAdd.brand` i18n en+ar; ui-rules/ui-tokens/ui-registry updated to the five-tile spec).

**SHIPPED (post-v1, 2026-06-13): Deal lifecycle redesign — `/architect`-planned, developer-approved, built + adversarially reviewed (build summary at the end of this paragraph; the agreed direction it implemented is recorded below).** Developer found the previous single-`deadline` + per-deliverable-"posted"-checkbox model confusing. Agreed direction (per-DEAL granularity, recommended over per-line): model a deal as a legible pipeline **To-do → Shot → Posted → Paid** with **two dates** (`shoot_date`, `post_date` — `deadline` becomes `post_date`) and **two checkmarks** (☐ Shot / ☐ Posted) that drive the status; the deliverables list stays as a read-only "what's owed" descriptor (loses its per-line checkboxes). **Two new auto-reminders** off the dates (shoot-day + post-day) so the dashboard **Today** panel becomes the influencer's daily worklist ("shoot X / post Y / chase payment Z"); ticking Shot clears the shoot reminder, ticking Posted clears the post reminder + arms the existing +24h "Capture Snap analytics" reminder. **Known design forks to resolve in `/architect`:** reminder `kind` CHECK must gain distinct `shoot`/`post` kinds (the upsert is one-row-per-(kind,ref_id), so shoot+post on the same deal id would collide under one kind); whether to add a `deal_shot` activity kind (closed-set change); backfill for existing deals (old deadline→post_date, old posted/in_progress→`posted_at`/`shot_at`); shoot/post dates optional vs required (lean optional); Needs-Attention becomes past-`post_date`-and-unposted (± past-`shoot_date`-and-unshot). Touches schema + status machine + reminders + deal form + expanded row + Today + Needs-Attention + i18n + every context doc — a full feature, hence `/architect` before code.

**BUILD SUMMARY (what shipped, 2026-06-13).** All four architect forks resolved as recommended: status `in_progress`→`shot` (keep `pending`, UI "To-do"); dates optional + Posted⇒Shot (back-stamps shot_at); ticking a checkmark DELETES that date's reminder, untick/edit RE-ARMS (createReminder upsert); Needs-attention = past-shoot-unshot OR past-post-unposted (post label wins). **Migration `0013_deal_lifecycle.sql`:** rename `deadline`→`post_date`; add `shoot_date`/`shot_at`/`posted_at`; backfill stamps from old deliverables + `in_progress`→`shot` (CHECK swap ordered **drop → convert → add** so neither the old nor the new check ever rejects an in-flight row); `reminders.kind` += `shoot`,`post`; `activity_log.kind` += `deal_shot` (`deliverable_posted` retired, kept in CHECK for history); index renamed; **`get_dashboard_stats` + `get_monthly_totals` recreated** for the column rename (definitions otherwise verbatim — invoiced/collected numbers unchanged). **Frontend:** `status.ts` rewritten (derive from stamps; `toggleShot`/`togglePosted`/`canToggleShot`/`becameShot`/`becamePosted`/`compareByPostDate`); `useDeals` = `useCreateDeal`+**`useUpdateDeal`** (edit flow, finally built; both sync shoot/post reminders, surface a soft `reminderFailed` toast) + `useMarkShot`/`useMarkPosted` (replace `useToggleDeliverable`; best-effort reminder housekeeping) + `useCancelDeal` (deletes the deal's reminders first); `Deliverable` type drops `posted_at`; `dueAt.ts` `dealDateReminderDueAt` + `messages.ts` shoot/post builders; `DealForm` two date inputs + create/edit; `DealExpandedPanel` rebuilt (read-only deliverables + ☐ Shot/☐ Posted + Edit sheet); `DealListItem`/`DealStatusPill`/`DealsFilters` use `shot`+`post_date`; ACTIVE-deal sets in deals/meetings/payments routes; dashboard `TodayPanel` (shoot/post kinds → /deals, info stripe, type badges) + `useBehindScheduleDeals` (PostgREST `.or()`) + `NeedsAttentionPanel`. Full EN+AR i18n; all context docs updated. typecheck + lint + production build clean (main ~763 kB). **Adversarial review (5-dimension workflow):** 7 findings, all verified — **6 fixed** (CRITICAL migration CHECK-order abort; the untracked seed updated to the new schema + shoot/post demo rows; un-Post now clears the stale snap reminder; shot_at/posted_at use `formatDualTimestampDate` not the UTC-pinned date formatter; snap-reminder-title-on-edit drift documented). **1 then RESOLVED (developer said "fix"):** `useCancelDeal` deleted reminders then flipped status non-atomically — a status-UPDATE failure after the deletes would strand an active deal with no reminders. **Fixed:** on UPDATE failure the cancel now re-arms the deal's shoot/post reminders (via `syncDealDateReminders`) + the snap reminder if posted (preserving its posted_at+24h window), then re-throws so the toast still fires. Deals-only — the meeting/payment cancel paths keep the plain delete-before-write pattern (their reminders aren't the worklist-critical date ones); the asymmetry is deliberate. **A `/review` pass** (separate from the build workflow) otherwise PASSED layers 1–2; its one Minor (edit-sheet brand `<select>` empty while `useBrands` loads) was also fixed (loading branch added); the `toDealFormInput` placement nit was left as-is (its inverse lives in the hook). **LIVE GATE (developer-gated):** apply `0013` (verify existing deals show correct stage + stamps, dashboard invoiced/reports monthly numbers UNCHANGED); create a deal with both dates → shoot/post reminders appear in Today → tick Shot (shoot reminder clears, `deal_shot` logged) → tick Posted (post reminder clears, +24h snap reminder arms, `deal_posted` logged) → untick re-arms / clears snap; edit a deal's dates → reminders move; cancel → reminders gone; Needs-attention shows past-shoot + past-post rows; 2nd-user RLS unaffected. Seed `seed_demo_data.sql` updated to the new schema for this gate.

**BUILT (post-v1, 2026-06-16): Brand categorization — `/architect`-planned, developer-approved, static-verified (typecheck + lint + production build clean). Pending its LIVE GATE.** A single, OPTIONAL category on each brand, chosen from a curated closed list of 18 industries (`restaurant`/`cafe`/`sweets`/`fashion`/`beauty`/`perfume`/`skincare`/`salon`/`wellness`/`fitness`/`clinic`/`jewelry`/`tech`/`home`/`travel`/`app`/`kids`/`other`). **Decisions (developer):** one category per brand; **hybrid** = curated keys + an `other` bucket (NO free-text — keeps the column a clean, bilingual, groupable set); category **optional** (`""` = Uncategorized → persists as `NULL`); existing brands stay **uncategorized, no backfill**; surfaces in the **form + a neutral row chip + the detail header** (a directory **filter is deferred**). **Migration `0014_brand_category.sql`:** `alter table brands add column category text` — **nullable, NO CHECK** (deliberate deviation from the status/kind CHECK convention: the set is product-curated and will grow, a CHECK would force a live-table migration per addition, and the value only ever comes from the closed `<select>` — enforced in app code instead); no RLS/grant change (existing `brands` policies cover the column). **Source of truth = `BRAND_CATEGORIES` const + `BrandCategory` union in `backend/shared/types/brand.types.ts`** (array order = picker order; the zod schema + the chip both read from it); `Brand` gains `category: BrandCategory | null`. **Frontend:** `brand.schema.ts` adds an optional `category` (refine: `""` or a member; backstop only) → `brands.errors.categoryInvalid`; `useBrands.toBrandColumns` maps `""`→`null`; `BrandForm` gains a `Select` (leading `categoryUnset` option + `BRAND_CATEGORIES.map`); `EMPTY_BRAND_FORM` (index route) + `brandToFormInput` (detail route) seed `category`; `BrandListItem` + the brand-detail header render the neutral category chip when set. **i18n** `brands.fields.category`/`categoryUnset` + `brands.categories.<key>` (18) + `brands.errors.categoryInvalid` (en+ar). No edge function, no atomic write, no new table. **LIVE GATE (developer-gated):** apply `0014`; add a brand with a category (chip shows on the row + detail), edit an existing uncategorized brand to set one (persists across reload), leave one Uncategorized (stays `NULL`, no chip); eyeball the `Select` + chip at 375px and in Arabic RTL. No new RLS test (no new table). To add a future category: append the key to `BRAND_CATEGORIES` + an EN/AR label under `brands.categories` — no migration.
- **REPLACED 2026-06-17:** the whole generic 18-key set was swapped for the developer's real Saudi small-business taxonomy — the picker is now **exactly their 31-item list, in their given order**. The 14 purely-generic keys were **REMOVED** (`restaurant`, `fashion`, `beauty`, `perfume`, `skincare`, `salon`, `wellness`, `fitness`, `jewelry`, `tech`, `home`, `travel`, `app`, `kids`); three were **kept + relabelled to the developer's exact wording** because their items map onto them (`cafe`→كافيه/Café, `clinic`→مستوصف/Clinic, `sweets`→محل حلا/Sweets — `sweets` EN dropped "Bakery &" to avoid overlap with the new `bakery` key); `other` stays last. **27 keys are new:** `barbershop`, `tailor`, `supermarket`, `pharmacy`, `restaurant_large/medium/small`, `mobiles`, `food_truck`, `entertainment`, `furniture`, `carpet`, `hotel`, `eyewear`, `chocolate`, `kunafa`, `grape_leaves`, `slimming`, `camp`, `butcher`, `laundry`, `juice`, `bakery`, `car_electrician`, `clothing`, `training`, `school`. **Still no migration / schema / component change** (`text` column, dynamic zod refine, `BrandForm` maps the array, `BrandListItem` resolves the label). `BRAND_CATEGORIES` + en/ar `brands.categories` all 31 keys, identical order; no code/seed referenced any removed key; typecheck + lint + build clean. **DATA CAVEAT (live):** removing keys orphans any existing brand row already tagged with one (its chip would show the raw key + it can't be re-selected in the form). The feature is still pre-live-gate so likely zero such rows; if any exist, re-tag them or run `update brands set category = null where category not in (<the 31 keys>);`.

**BUILT (post-v1, 2026-06-16): Delete brand + Delete meeting — `/architect`-planned, developer-approved, static-verified (typecheck + lint + production build clean). Pending its LIVE GATE.** Adds hard-delete for two entities; **no migration** (the `delete own` RLS policies already ship on both tables). **Decisions (developer):** (1) **Delete brand** is gated to brands with **zero deals** (the `ad_deals.brand_id` RESTRICT FK is the hard backstop; the UI pre-checks so the user sees a hint, not an FK error) — lives on the brand-detail page, disabled + a `brands.delete.blocked` hint when the brand has deals; on success → toast + navigate back to `/brands`. A deleted empty brand's meeting links sever via `meetings.brand_id` SET NULL (no second gate). (2) **Delete meeting REPLACES the soft Cancel** — a cancelled meeting was already excluded from every view and meetings carry no money/reporting lineage, so the row is removed outright; the `'cancelled'` status value stays in the enum for any existing rows. **Hooks:** new `useDeleteBrand(id)` (`useBrands.ts`; deletes the row, invalidates `BRANDS` + `MEETINGS` for the severed link); `useCancelMeeting` → **`useDeleteMeeting(meeting)`** (`useMeetings.ts`; clears the meeting's reminder via `deleteReminderForRef(MEETING)` then deletes the row — same delete-before-write order + accepted non-atomicity as the old cancel; invalidates `MEETINGS` + `REMINDERS`). **UI:** both reuse the existing two-step inline-confirm destructive pattern (`destructive` Button → [confirm | ghost keep]) — brand on the detail page (gated on the page's `useDealsForBrand` query: delete enabled only once it resolves with `deals.length === 0`), meeting in the edit sheet (the former cancel slot). **i18n** `brands.actions.{delete,confirmDelete,keepBrand}` + `brands.delete.blocked` + `brands.toast.{deleted,deleteError}`; meetings `actions.{cancelMeeting→deleteMeeting, confirmCancel→confirmDelete}` (keep `keepMeeting`) + `toast.{cancelled→deleted, cancelError→deleteError}` (en+ar). No new component → no `ui-registry.md` change. **LIVE GATE (developer-gated):** delete an empty brand (row gone → back on `/brands`, toast); open a brand WITH deals → delete disabled + hint; delete a meeting → gone from list/calendar + its reminder gone from Today (no more Cancel); eyeball both confirm rows at 375px + Arabic RTL; quick RLS spot-check that a 2nd user can't delete your brand/meeting.

**BUILT (post-v1, 2026-06-16): Twice-daily web-push reminders (PWA) — PHASE A of 2 — `/architect`-planned, developer-approved, static-verified (typecheck + lint + production build clean; PWA assets copied into `dist`). Pending its LIVE GATE.** A new notification *delivery* layer (distinct from the in-app `reminders` table): an installable PWA that will push the influencer a reminder twice a day (≈08:00 / 18:00 Riyadh) summarizing their own outstanding day (meetings + shoot/post due + payment awareness), sending nothing to users with nothing. **Decisions (developer):** web push now, **email fallback** designed into the seam for later; the WhatsApp-to-the-BRAND payment reminder is a SEPARATE parked feature (outbound to the client) — NOT this; both daily sends carry the same "today's outstanding" payload; hand-rolled PWA (no `vite-plugin-pwa`, **no offline/precaching** — avoids the stale-shell bug); permission requested on tap (not page-load); platform-aware install (real prompt on Android, instructions on iOS, where push needs an installed PWA); total cost $0 (VAPID + browser relays + pg_cron all free). **Split:** Phase A = subscribe + install (this build); **Phase B = the scheduled send (NOT yet built)** — `pg_cron`/`pg_net` → `send-daily-reminders` edge fn (service-role system job) → `get_users_with_outstanding` RPC → web push + prune dead subs; that's where VAPID secrets, `CRON_SECRET`, the cron schedule, and the `notification_sends` writer land.
**Phase A — what shipped:** migration **`0015_push_notifications.sql`** = `push_subscriptions` (endpoint UNIQUE for the subscribe upsert, `(user_id)` index) + `notification_sends` (idempotency log, UNIQUE `(user_id, slot, sent_on)` — no writer until Phase B, created now like F08's activity_log), both with the standard 4-policy own-row RLS + grants. Shared `backend/shared/types/pushSubscription.types.ts` (`PushSubscriptionRow` + `NOTIFICATION_SLOT`). Frontend: `VITE_VAPID_PUBLIC_KEY` added to `config/env.ts` as **optional** (blank ⇒ UI degrades to "unavailable", never throws). PWA assets in **new `frontend/public/`**: `manifest.webmanifest` (standalone, theme `#6e56f5`), minimal `sw.js` (push + notificationclick→`/dashboard`, empty fetch handler for install-eligibility, NO caching), placeholder icons (192/512/maskable-512/apple-touch — generated by `frontend/scripts/gen-icons.cjs`, **replace with real art**), manifest+theme+apple-touch links in `index.html`; SW registered on load in `main.tsx`. `lib/pushClient.ts` (SW register, `requestPermissionAndSubscribe`, `unsubscribeFromPush`, base64url→Uint8Array). Hooks `usePushNotifications` (capability + subscription state + enable/disable mutations that upsert/delete `push_subscriptions` via PostgREST — single-table write, no edge fn) + `useInstallPrompt` (captures `beforeinstallprompt`, detects iOS + standalone). UI `NotificationsSection` + `InstallAppButton` (registered in `ui-registry.md`), mounted in `/settings` after the Language section; `settings.notifications.*` i18n (en+ar). **LIVE GATE (developer-gated, partial — full delivery proves out in Phase B):** generate VAPID keys (`npx web-push generate-vapid-keys`), set `VITE_VAPID_PUBLIC_KEY` in `frontend/.env.local` (PRIVATE key stays out of the frontend — Supabase edge secret only, Phase B), apply `0015`; then on Android/desktop: enable notifications → permission prompt → a `push_subscriptions` row appears; install the PWA via the button; toggle off → row removed; iOS: confirm the install-required + instruction copy; eyeball the Notifications section at 375px + Arabic RTL; 2nd-user RLS spot-check on `push_subscriptions`.

**BUILT (post-v1, 2026-06-16): Twice-daily web-push reminders (PWA) — PHASE B (the scheduled send) — static-verified (`deno check` + `deno lint` clean on all edge functions; frontend untouched). Pending its LIVE GATE.** Completes the reminder feature: a scheduled job that pushes each user their "today's outstanding" digest twice a day, only to users who have something. **Library decision (verified against current docs, not memory): `jsr:@negrel/webpush@0.5.0`** — Web Crypto-native, runs cleanly on the Deno edge runtime (chosen over `npm:web-push`, which leans on Node's http/crypto stack and is unreliable there). It wants VAPID keys as a JWK pair; the project keeps the **base64url pair from `npx web-push generate-vapid-keys`** (so the same public key doubles as the browser `applicationServerKey`) and **converts base64→JWK in code** (`_shared/webpush.ts`: P-256 public = `0x04||X||Y`, private = 32-byte scalar `d`) — so the Phase-A keys are reused, no regeneration. **What shipped:** migration **`0016_daily_reminders_rpc.sql`** = RPC `get_users_with_outstanding()` (returns ONE row per user with ≥1 item today + per-category counts + locale; **security invoker, granted to `service_role` ONLY** — the one cross-tenant reader, locked by grant not RLS, since the service-role job bypasses RLS; computes "today" in `Asia/Riyadh`; driven off SOURCE tables — meetings upcoming now→end-of-day, deals pending+shoot_date≤today / pending|shot+post_date≤today, payments pending+expected_date≤today) + `create extension pg_cron`/`pg_net`. `backend/config/env.ts` gains lazy `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`/`CRON_SECRET` (same pattern as `OPENAI_API_KEY`). New `_shared/webpush.ts` (`getApplicationServer` — built once per invocation + cached; `sendPush` ttl 6h + per-slot `topic`; `isSubscriptionGone` → 410/404). Edge fn **`send-daily-reminders/index.ts`** = `x-cron-secret` header gate (deploy `--no-verify-jwt`) → zod `{slot}` → `createSupabaseAdmin` (service-role, the documented system/cross-user exception) → RPC → per user: CLAIM a `notification_sends` row first (upsert `ignoreDuplicates` on `(user_id,slot,sent_on)` → idempotent against cron retries; skip if already claimed) → build bilingual digest (`digest.ts`, EN/AR, copy lives in the fn since the edge runtime can't import the React catalog) → push to each `push_subscriptions` device → prune on gone → per-user try/catch so one failure can't sink the batch; returns `{usersNotified, pushesSent, pruned}`. **Cron + secrets are NOT committed** (they embed the project ref + `CRON_SECRET`) — documented as run-once steps in `backend/supabase/functions/send-daily-reminders/README.md` (08:00/18:00 Riyadh = 05:00/15:00 UTC cron via `net.http_post`). **LIVE GATE (developer-gated):** set the 4 edge secrets + apply `0016`; deploy `supabase functions deploy send-daily-reminders --no-verify-jwt`; enable pg_cron/pg_net (migration or Dashboard); add the two cron schedules; then `curl` the fn with `x-cron-secret` + `{"slot":"morning"}` → a push arrives on a Phase-A-subscribed device that has outstanding items today, nothing for an empty user, one `notification_sends` row per notified user; re-run same slot/day → no-op (idempotency); revoke a sub then run → it's pruned; check the Riyadh midnight boundary; confirm the `--no-verify-jwt` deploy + a wrong secret → 401. **Residual risk to confirm live:** the base64→JWK conversion feeding `importVapidKeys` — if a relay rejects the signature, fall back to regenerating keys with `@negrel/webpush`'s own generator (JWK-native). **Then the reminder feature is fully built; email fallback remains the documented next phase, plugging into the same `send-daily-reminders` loop alongside `sendPush`.**
**✅ LIVE-CONFIRMED 2026-06-17** — a real push landed on both macOS Chrome (FCM) and an iPhone (iOS 18.7, Apple relay) reading "Today's tasks · 1 meeting". Gotchas resolved during the live gate (all environmental, not logic — keep for next time): (1) a VAPID **private-key secret carried a stray char** → `atob` `InvalidCharacterError`; fixed by regenerating the pair cleanly (capture into shell vars, never hand-paste) **and** hardening `_shared/webpush.ts` `base64UrlToBytes` to strip non-base64url chars before decoding (committed). (2) **`VAPID_SUBJECT` was never set** — it's a required lazy getter, so it throws right after the keys; set `mailto:`. (3) Run `supabase` CLI from **`backend/`** (the only `supabase/config.toml`), NOT the repo root, or deploys/secrets silently target nothing. (4) **macOS suppresses Chrome notifications at the OS level** even when the web `Notification.permission` is `granted` — enable under System Settings → Notifications → Chrome (DevTools → Application → Service Workers → **Push** button is the fastest local display test). (5) **iOS web push requires the PWA installed from Safari** (Share → Add to Home Screen) — Chrome's "Add to Home Screen" on iOS yields a non-push-capable shortcut (Notification/showNotification are `undefined` there). (6) The long final red herring: a user only gets a push if **their own account** has outstanding items that day — devices were subscribed under one account while the test data sat under another (`pushesSent:0`, no log error). Test data must live under the subscribed account.
**Scheduling (developer-run): fixed 2 cron jobs at 10:00 & 18:00 Riyadh (07:00 & 15:00 UTC) — the developer runs `cron.schedule(...)` from the SQL editor per the function README; the times are NOT committed (they embed the project ref + CRON_SECRET). CRON_SECRET is currently a throwaway test value — rotate before real use.**
**PARKED / backlog — per-user reminder times (chosen in Settings instead of global fixed times).** Decided to ship the fixed-times version now and build this later. Architecture sketched (feasible, low-cost, reuses the whole engine): replace the 2 fixed cron jobs with ONE frequent heartbeat (every 15–30 min) → a new `get_users_due_now()` RPC returns users whose configured time matches the current window AND have outstanding items AND haven't been sent that slot today; the existing `notification_sends (user_id, slot, sent_on)` idempotency + the digest + push send all stay. Adds: `reminder_morning_at`/`reminder_evening_at time` on `app_users` (NULL = that slot off; default 10:00/18:00), two Settings time pickers (persist via `useUpdateAppUser`), and the cron→heartbeat swap. Cost is negligible (a 30-min tick = ~1,440 invocations/month ≈ 0.3% of the free tier; scales with tick frequency, not users; push delivery is free). Needs an `/architect` pass before building. Per-user timezone deferred (all users Riyadh).

**BUILT (post-v1, 2026-06-17): Deal shoot/post date+time — `/architect`-planned, developer-approved, static-verified (typecheck + lint + production build all clean). Pending its LIVE GATE.** A deal's PLANNED shoot and post now carry a **time of day**, not just a calendar day; the user sets one date+time per field when creating/editing. **Decisions (developer, 3-question round):** (1) **storage** = convert `ad_deals.shoot_date`/`post_date` `date` → **`timestamptz`** (one value each, mirroring `meetings.scheduled_at`; column names unchanged); (2) both stay **optional** — one `<input type="datetime-local">` per field, time included when set (all-or-nothing); (3) **reminders stay DAY-GRANULAR** — the stored time is shown + editable, but the shoot/post reminders, Needs-attention, and the twice-daily push digest still bucket by the planned **day**. The `shot_at`/`posted_at` completion stamps are untouched. **Timezone rule (key invariant):** every day/month bucket derives from the **Asia/Riyadh** calendar day of the timestamp (all users are Riyadh, as the push RPC already assumes) — so a 1 AM Riyadh value counts as that Riyadh day, not the UTC day. **Migration `0017_deal_datetimes.sql`:** `alter column … type timestamptz using (col::timestamp at time zone 'Asia/Riyadh')` (existing date-only rows → **Riyadh midnight**, faithful to the old "start of that day"; nulls stay null; the `(user_id,status,post_date)` index rebuilds automatically). Recreates **3 RPCs** so the new timestamps bucket the same as before: `get_dashboard_stats` + `get_monthly_totals` now compare/`date_trunc` on `(post_date at time zone 'Asia/Riyadh')::date` (invoiced/posted/pending by Riyadh-local month — numbers unchanged for date-origin data; the payments halves on `received_date`/`expected_date`, still `date`, untouched); `get_users_with_outstanding` (push digest) switches shoot/post from `<= today` to `< today_end` (the Riyadh end-of-today instant already in its CTE, matching the meetings predicate). No RLS/grant change (no new tables). **Frontend:** `deal.schema.ts` swaps the date regex for the `DATETIME_LOCAL_PATTERN` (reused from `meeting.schema`), both still optional; `DealForm` two inputs `type="date"`→`"datetime-local"`; `useDeals.toDealColumns` converts datetime-local→ISO UTC (`new Date(v).toISOString()`, `""`→null) and `monthRange` now returns viewer-local-midnight ISO bounds; `features/reminders/dueAt.ts` `dealDateReminderDueAt` derives the timestamp's **local day** then applies the existing start-of-local-day-or-now rule (kept import-free/pure; payments' date-based helper untouched) — daily behavior preserved. **Display** switched from the UTC-pinned `formatDualDate` to the local-tz `formatDualTimestampDate` + `formatTime`: deal card shows `date · time`, expanded-panel "Planned …" lines gain the time, the edit form prefills via `toDatetimeLocalValue`. **Day-comparison fixes for the type change:** `useNeedsAttention` compares against a **start-of-today (Riyadh) ISO instant** (double-quoted in the PostgREST `or()` since the instant contains `:`/`.`) instead of a bare date; `NeedsAttentionPanel` compares `localDayOfIso(post_date) < today`; `routes/deals.tsx` month-filter options bucket via `localDayOfIso(...).slice(0,7)`. **New `lib/date.ts` helpers:** `localDayOfIso` (viewer-local YYYY-MM-DD of a timestamp) + `startOfLocalDayIso` (local midnight of a YYYY-MM-DD as a UTC instant). i18n (en+ar): `deals.fields.shootDate/postDate` labels gain "& time", `deals.errors.dateInvalid` → "Enter a valid date and time", `deals.expanded.planned` gains `{{time}}`. `features/deals/status.ts` sort-by-post_date needs no change (ISO timestamps still sort lexically-chronologically). No edge-function code change (the push digest reads only the RPC counts). No new component → no `ui-registry` change. **LIVE GATE (developer-gated):** apply `0017`; create a deal with a shoot + post date+time → card shows `date · time`, expanded panel shows the time, edit prefills the exact datetime-local value and round-trips; a future-day shoot/post lands in the Today worklist on the planned **day** (daily, not at the minute); a past-day unshot/unposted deal shows under Needs-attention; the **dashboard + reports month numbers are unchanged** vs before the migration; eyeball the two `datetime-local` pickers at 375px + Arabic RTL (Western digits in the input by design). **Heads-up:** the untracked local-only `backend/supabase/tests/seed_demo_data.sql` uses date literals for shoot/post — they cast cleanly to timestamptz if re-run, no edit needed.

**BUILT (post-v1, 2026-06-17): "Already received?" toggle on the Add-payment form — static-verified (typecheck + lint + production build all clean). NO schema/migration/edge-fn change. Pending its LIVE GATE.** Motivation: brands often **pay in advance to reserve a spot**, so the influencer needs to record money that's *already in* while adding the payment — instead of the old two-step (create pending → mark received from the Pending tab). **Decision (key modeling point):** "already paid" is NOT a payment *method* — `method` (cash/bank/other) is *how* the money came; received is a *separate axis* (the `status` column). So the form keeps the **method `Select` (cash/bank/other, unchanged — "Other" kept, not dropped)** and adds a **separate "Already received?" checkbox** rather than merging them (a received cash payment must still record "cash"). **What changed (frontend-only):** `payment.schema.ts` gains `markReceived: z.boolean()`; `PaymentForm` adds the checkbox (reuses `DealExpandedPanel`'s canonical native pattern — `label flex min-h-11 cursor-pointer items-center gap-3` + `input type=checkbox size-5 shrink-0 accent-accent` + `flex-1 text-sm` span) and **hides the expected-date field when the toggle is on** (`watch("markReceived")` — a received payment is stamped by the RPC, so an expected date is moot); field order is now deal → amount → method → already-received → expected-date → notes. **`useCreatePayment` now returns `{payment, markedReceived, markFailed, dealPaid}`:** it inserts the row (always pending — the insert path never writes `received`), then when `markReceived` it routes the new id through the **existing `mark-payment-received` edge fn → `mark_payment_received` RPC** (the ONLY writer of `received` + the deal `paid` flip — never a direct status write). **Best-effort mark (the one correctness call):** if the RPC fails *after* the insert, it does NOT throw (a retry would create a duplicate row) — it logs, returns `markFailed:true`, and the route shows a soft `payments.toast.createdNotReceived` ("saved — mark it received from Pending"), mirroring `useUpdateDeal`'s `reminderFailed` pattern. Invalidations moved to **`onSettled`** (new row must show even on partial failure) and now also invalidate **DEALS** (the deal may have flipped to paid). Route `handleCreate` picks the toast by outcome (created / received / dealPaid / createdNotReceived); `EMPTY_PAYMENT_FORM` seeds `markReceived:false`. New i18n (en+ar): `payments.fields.alreadyReceived`, `payments.toast.createdNotReceived`. Bundle main ~777.6 kB (+~1.4 kB). **The reservation/advance flow end to end:** create the deal (scope TBD, stays `pending`) → Add payment → amount + Cash/Bank + tick "Already received?" → money lands in the Received tab + counts as collected; the balance is a later payment with the toggle OFF, and receiving it flips the deal to `paid`. **LIVE GATE (developer-gated):** add a payment with "Already received?" ON → it lands directly in **Received** (not Pending), the dashboard "collected" rises, and if it was the deal's only/last installment the deal shows **Paid**; add one with the toggle OFF → lands in **Pending** with its expected date; eyeball the checkbox at 375px + Arabic RTL.

---

## Notes

_Add notes here as the build progresses — workarounds, patterns, anything that differs from the context files._

- Standards sweep at the end of every chunk: no `console.log`, no `any`, no inline magic values, one response envelope + consistent HTTP status codes across edge functions.
- Verification gate for every data-bearing chunk: second-user RLS test must pass before "done."
- Mobile-first: every page usable at 375px width (dry-run for the future React Native port).
- **Runtime env required to boot:** `frontend/.env.local` must define `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` (developer-managed; never committed). `config/env.ts` validates presence at startup and throws a clear `[env] Missing required environment variable: …` if absent.
- Feature 01 verified by typecheck (`tsc -b`) + lint (`eslint`) + production build — all clean. Runtime redirect behavior (signed-in → `/dashboard`, signed-out → `/login`) is pending the developer's Supabase keys in `.env.local`; the logged-out path was not yet exercised live.
- **Feature 02 verified statically** (typecheck + lint + build all clean). **NOT yet run live** — needs: (1) `frontend/.env.local` with Supabase keys, (2) `0001_app_users.sql` applied to the Supabase project, (3) Google provider + redirect URLs configured in the Supabase dashboard. **Open acceptance gates:** second-user RLS test on `app_users`, the full Google + email/password + email-verify flows, and the 375px visual pass on the login screen.
